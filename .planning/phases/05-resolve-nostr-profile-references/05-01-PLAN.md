---
phase: 05-resolve-nostr-profile-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/org/dergigi/boris/nostr/Nip19.kt
  - app/src/main/java/org/dergigi/boris/data/NostrLink.kt
  - app/src/main/java/org/dergigi/boris/data/NostrMentions.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
  - app/src/main/java/org/dergigi/boris/data/UrlExtractor.kt
  - app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
  - app/src/main/java/org/dergigi/boris/data/ReadingPositionStore.kt
  - app/src/main/java/org/dergigi/boris/data/ArticleUrl.kt
  - app/src/main/java/org/dergigi/boris/data/ContinueReading.kt
  - app/src/main/java/org/dergigi/boris/data/ArchivedArticles.kt
  - app/src/main/java/org/dergigi/boris/data/ArticlePreview.kt
  - app/src/main/java/org/dergigi/boris/data/RandomArticles.kt
  - app/src/main/java/org/dergigi/boris/data/HighlightedArticles.kt
  - app/src/main/java/org/dergigi/boris/data/OfflineDownloader.kt
  - app/src/main/java/org/dergigi/boris/nostr/Nip85.kt
  - app/src/main/java/org/dergigi/boris/tts/TtsText.kt
  - app/src/test/java/org/dergigi/boris/nostr/Nip19Test.kt
  - app/src/test/java/org/dergigi/boris/data/NostrLinkTest.kt
  - app/src/test/java/org/dergigi/boris/data/NostrMentionsTest.kt
  - app/src/test/java/org/dergigi/boris/ui/reader/ReaderLinksTest.kt
  - app/src/test/java/org/dergigi/boris/data/UrlExtractorTest.kt
  - app/src/test/java/org/dergigi/boris/tts/TtsTextTest.kt
autonomous: true
requirements:
  - READ-03
  - READ-01
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01: Raw prefixed nprofile and npub mentions paint as tappable @name using Profile.displayName, short npub until kind 0 is cached"
    - "D-02: Text only; existing textLink color; no mention composable"
    - "D-03: Custom markdown labels stay as the author wrote them; only raw prefixed mentions become @name"
    - "D-04: Tap opens Routes.profile via OpenProfile for both nprofile and npub, logged out"
    - "D-05: note, nevent, naddr keep today's reader routing; secret-key bech32 is never a profile"
    - "D-06: nprofile TLV type 0 is required; type 1 hints are decoded onto Profile.relays; a bad hint cannot drop the pubkey"
    - "D-07: Profile.relays is the input for fetch-union in 05-02"
    - "D-08: Profile.relays is the input for persist in 05-02"
    - "D-09: Bare nprofile1 / npub1 without nostr: or nostr:// stay plaintext"
    - Profile URIs never become article fetch, Home extract, or Jina (READ-01)
    - Invalid identifiers stay truncated plaintext, not a crash; TTS speaks the link label after rewrite
    - Empty: zero mentions, article paints as today. Loading: short npub at first paint, no spinner, no markdown remount. Error: undecodable identifier is truncated body text
  artifacts:
    - path: app/src/main/java/org/dergigi/boris/nostr/Nip19.kt
      provides: nprofileDecode / nprofileEncode / NprofilePointer / normalizePubkey nprofile (D-06)
      exports: ["NprofilePointer", "nprofileDecode", "nprofileEncode"]
    - path: app/src/main/java/org/dergigi/boris/data/NostrLink.kt
      provides: NostrTarget.Profile plus prefixed nprofile/npub parse (D-04, D-05, D-09)
      exports: ["NostrTarget.Profile"]
    - path: app/src/main/java/org/dergigi/boris/data/NostrMentions.kt
      provides: rewrite raw prefixed mentions to [@name](nostr:…) (D-01, D-02, D-03, D-09)
      exports: ["NostrMentions"]
    - path: app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt
      provides: OpenProfile before UrlExtractor.articleUrl (D-04, READ-01)
      exports: ["ReaderLinkAction.OpenProfile", "readerLinkAction"]
  key_links:
    - from: app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
      to: app/src/main/java/org/dergigi/boris/data/NostrMentions.kt
      via: markdownBody = Footnotes.expand then NostrMentions.rewrite, keyed only on content.body
      pattern: NostrMentions.rewrite
    - from: app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
      to: app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt
      via: UriHandler OpenProfile calls onOpenProfile(hex)
      pattern: OpenProfile
    - from: app/src/main/java/org/dergigi/boris/data/UrlExtractor.kt
      to: app/src/main/java/org/dergigi/boris/data/NostrLink.kt
      via: extract and articleUrl return uri only for Article and Note
      pattern: NostrTarget.Profile
  prohibitions:
    - statement: Do not bump versionCode, versionName, CHANGELOG, or publish Zapstore
      status: resolved
      verification: explicit
    - statement: Do not add Gradle libraries
      status: resolved
      verification: explicit
    - statement: Do not add a mention composable, avatar, chip, or new link color
      status: resolved
      verification: explicit
    - statement: Do not autolink bare bech32 without a nostr scheme
      status: resolved
      verification: explicit
    - statement: Do not treat note, nevent, naddr, or secret-key bech32 as profile mentions
      status: resolved
      verification: explicit
    - statement: Do not remount rememberMarkdownState when kind 0 arrives
      status: resolved
      verification: explicit
    - statement: Do not mutate ReadableContent.body; rewrite is a display/TTS view
      status: resolved
      verification: explicit
    - statement: Do not gate mention taps or reading on SessionStore
      status: resolved
      verification: explicit
---

## Phase Goal

`nostr:nprofile` references in article Markdown render as tappable profile links that open the in-app profile screen, without breaking existing note, event, and article nostr links.

<objective>
Prove the mention path end-to-end: decode nprofile/npub, rewrite raw prefixed mentions to [@name](nostr:…), route taps to the existing profile screen, and keep profile URIs out of article fetch. D-01: @name via Profile.displayName. D-02: text only. D-03: keep custom labels. D-04: nprofile and npub OpenProfile. D-05: not note/nevent/naddr/secret-key. D-06: TLV hints on Profile.relays. D-07: relays field is fetch-union input. D-08: relays field is persist input. D-09: prefix required. READ-03 READ-01. Hinted-relay persist and fetch union land in 05-02; this plan still decodes type-1 relays onto NostrTarget.Profile.relays so that plan has data to remember. TTS speaks @name because rewrite runs before TtsText flatten-to-labels.
Purpose: Raw nostr:nprofile is plaintext today because GFM does not autolink the scheme and entityRegex has no profile HRPs. A UriHandler-only change never fires.
Output: nprofileDecode, NostrTarget.Profile, NostrMentions.rewrite, OpenProfile routing, exhaustive Profile when arms, JVM tests
</objective>

<assumption_delta_decision>
noun: mention rewrite
decision: new-assumption
rationale: Pre-process markdown after Footnotes.expand (webapp replaceNostrUrisSafely shape, prefix required). Do not add a mikepenz annotator or a new composable.
</assumption_delta_decision>

<execution_context>
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-resolve-nostr-profile-references/05-CONTEXT.md
@.planning/phases/05-resolve-nostr-profile-references/05-RESEARCH.md
@.planning/phases/05-resolve-nostr-profile-references/05-PATTERNS.md
@.planning/phases/05-resolve-nostr-profile-references/05-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@app/src/main/java/org/dergigi/boris/nostr/Nip19.kt
@app/src/main/java/org/dergigi/boris/data/NostrLink.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
@app/src/main/java/org/dergigi/boris/data/UrlExtractor.kt
@app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
@app/src/main/java/org/dergigi/boris/nostr/Profile.kt
@app/src/main/java/org/dergigi/boris/tts/TtsText.kt
@app/src/main/java/org/dergigi/boris/data/Footnotes.kt

<interfaces>
From app/src/main/java/org/dergigi/boris/nostr/Nip19.kt:
Copy neventDecode / neventEncode. Add data class NprofilePointer(pubkey: String, relays: List&lt;String&gt; = emptyList()). nprofileDecode: HRP nprofile, type 0 = 32-byte pubkey hex length 64, type 1 = UTF-8 relay strings, extra types ignored. Do not sanitize relays inside decode (D-06). nprofileEncode: type 0 pubkey bytes, type 1 relay UTF-8. normalizePubkey: trimmed.startsWith("nprofile1") -&gt; nprofileDecode(trimmed).pubkey, still in the existing try/catch.

From app/src/main/java/org/dergigi/boris/data/NostrLink.kt:
Sealed NostrTarget currently Article and Note. Add Profile(pubkeyHex, encoded, relays) with uri "nostr:$encoded" and publicUrl gatewayUrl(encoded), copying Note. decode: nprofile1 -&gt; nprofileDecode then LocalRelays.resolve each relay (drop nulls, keep pubkey); npub1 -&gt; npubDecode, empty relays. Unknown HRPs stay null. entityRegex: naddr/note/nevent keep optional scheme; nprofile/npub require nostr: or nostr:// (D-09 share-text safety). Reject identifiers longer than 5000 characters before decode.

From app/src/main/java/org/dergigi/boris/nostr/Profile.kt (do not edit):
displayName(pubkeyHex, profile) then shortNpub (npub.take(12) + ellipsis). Use this for D-01. Do not copy the webapp slice(5, 12) fallback.

From app/src/main/java/org/dergigi/boris/ui/BorisApp.kt (do not edit):
Routes.profile(npub) = "profile/$npub". onOpenProfile already npubEncodes and navigates. Login-free (READ-01).

From app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt:
onOpenProfile(String) already exists. UriHandler when must gain OpenProfile. markdownBody remember(content.body) currently Footnotes.expand only. textLink is body.copy(color = linkColor).toSpanStyle() — mentions must use that path, no new SpanStyle.

From app/src/main/java/org/dergigi/boris/data/Footnotes.kt:
protectCode is private. Copy the stash/restore (NUL index tokens, FENCE, INLINE_CODE) into NostrMentions.

From app/src/main/java/org/dergigi/boris/tts/TtsText.kt:
paragraphs runs Footnotes.expand then splitMarkdownBlocks then MarkdownInline.plain. Insert NostrMentions.rewrite after expand so spoken text is the label.

From app/src/main/java/org/dergigi/boris/nostr/LocalRelays.kt (do not edit the resolver):
resolve(url) = canonical(url) ?: Nip66.normalize(url). Use after a successful pubkey decode.

Gradle: always ./gradlew --offline (dl.google.com is unreachable). JUnit 4 under app/src/test/java/org/dergigi/boris/. No Robolectric. CONVENTIONS: org.dergigi.boris only, no KDoc, no TODO, no Log.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end prefixed nprofile mention to in-app profile</name>
  <files>app/src/main/java/org/dergigi/boris/nostr/Nip19.kt, app/src/main/java/org/dergigi/boris/data/NostrLink.kt, app/src/main/java/org/dergigi/boris/data/NostrMentions.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt, app/src/main/java/org/dergigi/boris/data/UrlExtractor.kt, app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt, app/src/main/java/org/dergigi/boris/data/ReadingPositionStore.kt, app/src/main/java/org/dergigi/boris/data/ArticleUrl.kt, app/src/main/java/org/dergigi/boris/data/ContinueReading.kt, app/src/main/java/org/dergigi/boris/data/ArchivedArticles.kt, app/src/main/java/org/dergigi/boris/data/ArticlePreview.kt, app/src/main/java/org/dergigi/boris/data/RandomArticles.kt, app/src/main/java/org/dergigi/boris/data/HighlightedArticles.kt, app/src/main/java/org/dergigi/boris/data/OfflineDownloader.kt, app/src/main/java/org/dergigi/boris/nostr/Nip85.kt, app/src/main/java/org/dergigi/boris/tts/TtsText.kt</files>
  <read_first>.planning/phases/05-resolve-nostr-profile-references/05-CONTEXT.md, .planning/phases/05-resolve-nostr-profile-references/05-RESEARCH.md, .planning/phases/05-resolve-nostr-profile-references/05-PATTERNS.md, .planning/phases/05-resolve-nostr-profile-references/05-UI-SPEC.md, app/src/main/java/org/dergigi/boris/nostr/Nip19.kt, app/src/main/java/org/dergigi/boris/data/NostrLink.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt, app/src/main/java/org/dergigi/boris/data/UrlExtractor.kt, app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt, app/src/main/java/org/dergigi/boris/nostr/Profile.kt, app/src/main/java/org/dergigi/boris/tts/TtsText.kt, app/src/main/java/org/dergigi/boris/data/Footnotes.kt</read_first>
  <action>
Implement one production mention path under org.dergigi.boris only. D-01: D-02: D-03: D-04: D-05: D-06: D-07: D-08: D-09: Do not recreate com.readwithboris. Do not add Gradle libraries. Do not bump versionCode or versionName. Do not add a mention composable, avatar, chip, spinner, or a second link color; mentions are markdown links using the existing textLink SpanStyle (D-02, 05-UI-SPEC). Follow CONVENTIONS.md: no android.util.Log or Timber, no KDoc, no TODO. Never request, persist, or decode a user private key. Do not call SessionStore from rewrite or readerLinkAction (READ-01). The 17-file count is accepted: adding Profile without every when(NostrTarget) arm fails compile, so the sweep ships with the tracer.

1. Nip19 (D-06): NprofilePointer(pubkey, relays). nprofileDecode mirrors neventDecode with HRP nprofile, type 0 pubkey length 64, type 1 relay strings, extra TLV types ignored. Missing or short type 0 throws. Garbage type 1 must not throw; return the strings as decoded. Do not call LocalRelays inside decode. nprofileEncode mirrors neventEncode types 0 and 1. normalizePubkey accepts nprofile1 via nprofileDecode.pubkey.

2. NostrLink (D-04, D-05, D-09): NostrTarget.Profile(pubkeyHex, encoded, relays) copying Note's uri/publicUrl. decode nprofile1 and npub1 before the else-null branch. After nprofileDecode, map relays through LocalRelays.resolve and drop nulls; keep the pubkey even if every hint is dropped (D-06). npub has empty relays. entityRegex: keep optional scheme for naddr1|note1|nevent1; require nostr: or nostr:// before nprofile1|npub1 using the same bech32 charset class already in entityRegex. Do not add a secret-key HRP. Identifiers longer than 5000 characters are not decoded. catch (_: Exception) { null } stays.

3. Exhaustive when(NostrTarget) — Profile is not an article (READ-01). Copy the skip style from Nip85 Note -&gt; null. ReaderRepository.fetch: throw IOException with a short human-readable message; do not fall through to the Jina branch. UrlExtractor.extract and articleUrl (both parse sites, including the later parse(absolute)): return uri only for Article and Note; Profile skips. ReadingPositionStore.key: Profile -&gt; target.uri. ArticleUrl.host: Profile -&gt; null. ContinueReading host: Profile -&gt; continue. ArchivedArticles.key(url): Profile -&gt; null. ArticlePreview.keysFor: Profile -&gt; Unit. RandomArticles host: Profile -&gt; item.host ?: url. HighlightedArticles fromEvents / mostHighlighted / hydrate / decorate: skip (continue, mapNotNull null, Unit, or return article unchanged). OfflineDownloader.estimatedBytes: Profile -&gt; 0L. Nip85.dTag: Profile -&gt; null. ArticleUrl.root already returns null for any successful parse; leave that.

4. NostrMentions object in data/ (D-01, D-02, D-03, D-09). fun rewrite(markdown: String): String. Do not mutate ReadableContent.body. Copy Footnotes.protectCode. Walk ]( … matching ) with depth and skip matches whose range sits inside those URL spans (D-03). Regex prefix required, nprofile1 or npub1 only, IGNORE_CASE, same charset as entityRegex, lookbehind not slash or word char, matching 05-RESEARCH Pattern 2. Decode nprofile via nprofileDecode or npub via npubDecode; anything else leave the match. Valid: label is "@" + Profile.displayName(pubkeyHex, EventCache.latest(KIND_METADATA, pubkey)?.let { Profile.parse(it.content) }); href stays nostr:$encoded (nprofile stays nprofile). Invalid / undecodable: replace the match with the first 20 characters of the original match plus the same ellipsis character Profile.shortNpub uses; not a markdown link; do not crash (UI-SPEC). Bare nprofile1 / npub1 without scheme stay plaintext (D-09). note / nevent / naddr / secret-key URIs are not this regex (D-05). If a match is wrapped in angle brackets, emit the markdown link without the brackets so CommonMark autolinks still become @name. Do not persist relays here; Profile.relays is enough for 05-02 (D-07, D-08).

5. readerLinkAction (D-04, READ-01): add data class OpenProfile(pubkeyHex: String). Parse NostrLink FIRST. Profile returns OpenProfile(pubkeyHex) before UrlExtractor.articleUrl. Article, Note, and null fall through to today's Ignore / OpenInReader / OpenExternal. openWeblink when: OpenProfile -&gt; Unit (settings weblinks are http). ReaderScreen UriHandler: OpenProfile -&gt; onOpenProfile(action.pubkeyHex); put onOpenProfile in the remember keys. Do not restyle ProfileScreen.

6. ReaderScreen markdownBody: remember(content.body) { Footnotes.expand then NostrMentions.rewrite }. Do not key on EventCache or kind 0 (no remount, no spinner).

7. TtsText.paragraphs: rewrite after Footnotes.expand so MarkdownInline.plain speaks @name or the custom label, not the bech32 blob.

8. After verify passes, commit only this task's production files with: feat(05-01): resolve prefixed nostr profile mentions in the reader
  </action>
  <verify>
    <automated>./gradlew --offline :app:assembleDebug</automated>
    <human-check>Logged out: open the issue 5 repro article (naddr in 05-CONTEXT). A raw nostr:nprofile paints as @ plus short npub or a cached display name, same color as other markdown links, no avatar. Tap opens the in-app profile. A custom [Gigi](nostr:nprofile…) stays Gigi. Existing naddr/note/nevent links still open as today. Home paste of a profile URI does not open the reader.</human-check>
  </verify>
  <acceptance_criteria>
    - Nip19.kt contains nprofileDecode and nprofileEncode and NprofilePointer
    - NostrLink.kt contains data class Profile and nprofile1
    - NostrMentions.kt contains fun rewrite
    - ReaderLinks.kt contains OpenProfile and NostrLink.parse
    - ReaderScreen.kt contains OpenProfile and NostrMentions.rewrite
    - TtsText.kt contains NostrMentions.rewrite
    - ReaderRepository.kt contains is NostrTarget.Profile
    - UrlExtractor.kt contains is NostrTarget.Profile
    - ReadingPositionStore.kt contains is NostrTarget.Profile
    - ArticleUrl.kt contains is NostrTarget.Profile
    - ContinueReading.kt contains is NostrTarget.Profile
    - ArchivedArticles.kt contains is NostrTarget.Profile
    - ArticlePreview.kt contains is NostrTarget.Profile
    - RandomArticles.kt contains is NostrTarget.Profile
    - HighlightedArticles.kt contains is NostrTarget.Profile
    - OfflineDownloader.kt contains is NostrTarget.Profile
    - Nip85.kt contains is NostrTarget.Profile
    - grep -vE '^[[:space:]]*(//|#|\*)' app/src/main/java/org/dergigi/boris/data/NostrLink.kt | grep -c 'nsec1' is 0
    - grep -vE '^[[:space:]]*(//|#|\*)' app/src/main/java/org/dergigi/boris/data/NostrMentions.kt | grep -c 'nsec1' is 0
    - ./gradlew --offline :app:assembleDebug exits 0
  </acceptance_criteria>
  <done>Debug APK builds. Prefixed nprofile/npub mentions rewrite to @name links, taps open the existing profile route logged out, Profile URIs cannot become article fetch, and every NostrTarget when has a Profile arm.</done>
</task>

<task type="auto">
  <name>JVM tests for decode, rewrite, routing, and extract skip</name>
  <files>app/src/test/java/org/dergigi/boris/nostr/Nip19Test.kt, app/src/test/java/org/dergigi/boris/data/NostrLinkTest.kt, app/src/test/java/org/dergigi/boris/data/NostrMentionsTest.kt, app/src/test/java/org/dergigi/boris/ui/reader/ReaderLinksTest.kt, app/src/test/java/org/dergigi/boris/data/UrlExtractorTest.kt, app/src/test/java/org/dergigi/boris/tts/TtsTextTest.kt</files>
  <read_first>app/src/test/java/org/dergigi/boris/nostr/Nip19Test.kt, app/src/test/java/org/dergigi/boris/data/NostrLinkTest.kt, app/src/test/java/org/dergigi/boris/ui/reader/ReaderLinksTest.kt, app/src/test/java/org/dergigi/boris/data/UrlExtractorTest.kt, app/src/test/java/org/dergigi/boris/tts/TtsTextTest.kt, app/src/test/java/org/dergigi/boris/data/FootnotesTest.kt</read_first>
  <action>
Add JUnit 4 tests matching neighboring style (org.junit.Assert, camelCase names, no Robolectric, no androidTest). Do not add packages. Use ./gradlew --offline. D-01: D-03: D-04: D-05: D-06: D-09: pin fixtures from 05-RESEARCH.

Nip19Test: official NIP-19 nprofile vector nprofile1qqsrhuxx8l9ex335q7he0f09aej04zpazpl0ne2cgukyawd24mayt8gpp4mhxue69uhhytnc9e3k7mgpz4mhxue69uhkg6nzv9ejuumpv34kytnrdaksjlyr9p decodes to pubkey 3bf0c63fcb93463407af97a5e5ee64fa883d107ef9e558472c4eb9aaaefa459d and relays wss://r.x.com plus wss://djbas.sadkb.com. Round-trip encode/decode. Extra unknown TLV ignored. Type-1 garbage still returns pubkey (D-06). normalizePubkey(nprofile) equals that pubkey. nprofileDecode of an npub throws. Secret-key HRP is rejected (throws or is not nprofile).

NostrLinkTest: nostr:nprofile and nostr://nprofile -&gt; NostrTarget.Profile; nostr:npub -&gt; Profile (D-04). naddr/note/nevent unchanged. nostr: plus the official NIP-19 secret-key vector -&gt; null (D-05). Bare nprofile1 / npub1 without scheme -&gt; null (D-09). Issue 5 nprofile nprofile1qyv8wue69uhk6mmwv9jzu6nzx56jucm0d5arsvpcxqq3qamn8ghj7atdvfex2mp6xsurgwqqyzdkm9dhdgq3jxjvw7qc26q76lememf0l75wg9gka3uzgzepx2zl2ewxw6s prefixed with nostr: parses as Profile.

NostrMentionsTest: raw nostr:nprofile becomes a markdown link whose label is "@" + Profile.displayName and whose href is nostr: plus the encoded id (D-01). [Gigi](nostr:nprofile1…) is unchanged (D-03). Bare nprofile1 / npub1 unchanged (D-09). nostr: plus secret-key vector unchanged. nostr:naddr1 unchanged (D-05). Fenced and inline code unchanged. Undecodable prefixed nprofile becomes 20-character truncated plaintext, not a link.

ReaderLinksTest: nprofile/npub -&gt; OpenProfile(hex) (D-04). naddr still OpenInReader when openInReader is true. mailto still OpenExternal. Secret-key URI is not OpenProfile (D-05).

UrlExtractorTest: articleUrl("nostr:nprofile…") is null; extract("nostr:npub1…") is null (READ-01). Existing naddr extract stays green. Issue 5 repro naddr still extracts.

TtsTextTest: a body containing a raw nostr:npub mention speaks the @ displayName label, not the bech32 blob (flatten-links-to-labels after rewrite).

After verify passes, commit only this task's test files with: test(05-01): cover nprofile decode, rewrite, and routing
  </action>
  <verify>
    <automated>./gradlew --offline :app:testDebugUnitTest --tests org.dergigi.boris.nostr.Nip19Test --tests org.dergigi.boris.data.NostrLinkTest --tests org.dergigi.boris.data.NostrMentionsTest --tests org.dergigi.boris.ui.reader.ReaderLinksTest --tests org.dergigi.boris.data.UrlExtractorTest --tests org.dergigi.boris.tts.TtsTextTest</automated>
  </verify>
  <acceptance_criteria>
    - Nip19Test.kt contains nprofileDecode and 3bf0c63fcb93463407af97a5e5ee64fa883d107ef9e558472c4eb9aaaefa459d
    - NostrLinkTest.kt contains NostrTarget.Profile and nprofile1qyv8wue69uhk6mmwv9jzu6nzx56jucm0d5arsvpcxqq3qamn8ghj7atdvfex2mp6xsurgwqqyzdkm9dhdgq3jxjvw7qc26q76lememf0l75wg9gka3uzgzepx2zl2ewxw6s
    - NostrMentionsTest.kt contains rewrite and Gigi
    - ReaderLinksTest.kt contains OpenProfile
    - UrlExtractorTest.kt contains nprofile
    - TtsTextTest.kt contains NostrMentions or npub1 or @
    - ./gradlew --offline :app:testDebugUnitTest --tests org.dergigi.boris.nostr.Nip19Test --tests org.dergigi.boris.data.NostrLinkTest --tests org.dergigi.boris.data.NostrMentionsTest --tests org.dergigi.boris.ui.reader.ReaderLinksTest --tests org.dergigi.boris.data.UrlExtractorTest --tests org.dergigi.boris.tts.TtsTextTest exits 0
  </acceptance_criteria>
  <done>Decode, rewrite, OpenProfile routing, extract-skip, and TTS label tests pass on the JVM with --offline.</done>
</task>

</tasks>

<threat_model asvs="1" block_on="high">

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReadableContent.body → NostrMentions.rewrite | Untrusted article markdown becomes link labels and hrefs |
| LocalUriHandler.openUri → readerLinkAction | Untrusted tap URIs choose in-app profile vs article vs external |
| NostrLink.parse → UrlExtractor / ReaderRepository.fetch | A profile URI must not become Jina or reader content |
| Bech32 decode | Wrong HRP must not be treated as a pubkey |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | NostrLink.decode / NostrMentions.rewrite | high | mitigate | Do not add a secret-key HRP to entityRegex or PROFILE_MENTION; parse returns null; tests use the official NIP-19 secret-key vector (D-05) |
| T-05-02 | Tampering | UrlExtractor / ReaderRepository.fetch | high | mitigate | Profile skip in extract and articleUrl; ReaderRepository Profile arm throws IOException and never hits Jina (READ-01) |
| T-05-03 | Tampering | NostrMentions.rewrite | medium | mitigate | Labels come from Profile.displayName only; href is nostr: plus the matched bech32; skip code fences and ](url) destinations (D-03) |
| T-05-04 | Denial of Service | Nip19 / NostrLink | medium | mitigate | Reject identifiers longer than 5000 characters; invalid decode stays truncated plaintext |
| T-05-05 | Elevation of Privilege | readerLinkAction | high | mitigate | No SessionStore check; OpenProfile uses existing login-free Routes.profile (READ-01) |
| T-05-SC | Tampering | Gradle installs | high | mitigate | No new packages; Package Legitimacy Audit is empty |

</threat_model>

<verification>
- [ ] ./gradlew --offline :app:assembleDebug exits 0
- [ ] ./gradlew --offline :app:testDebugUnitTest --tests org.dergigi.boris.nostr.Nip19Test --tests org.dergigi.boris.data.NostrLinkTest --tests org.dergigi.boris.data.NostrMentionsTest --tests org.dergigi.boris.ui.reader.ReaderLinksTest --tests org.dergigi.boris.data.UrlExtractorTest --tests org.dergigi.boris.tts.TtsTextTest exits 0
- [ ] Every when(NostrTarget) has an is NostrTarget.Profile arm
- [ ] readerLinkAction parses NostrLink before UrlExtractor.articleUrl
- [ ] Home paste/share of a profile URI cannot open the reader (READ-01)
</verification>

<success_criteria>
- READ-03: prefixed nprofile/npub mentions render as @name profile links that open the in-app profile; naddr/note/nevent still work; login is not required
- READ-01: paste, share, or open a URL and read while logged out; a profile URI never becomes article fetch
- D-01 D-02 D-03: @name text only; custom labels kept; short npub until cached kind 0
- D-04 D-05 D-06 D-09: both nprofile and npub; not note/nevent/naddr/secret-key; TLV hints decoded without dropping pubkey; prefix required
- D-07 D-08: Profile.relays carries decoded hints for 05-02 persist
</success_criteria>

## Artifacts this phase produces

- NprofilePointer + nprofileDecode/Encode (D-06)
- NostrTarget.Profile (D-04)
- NostrMentions.rewrite (D-01, D-02, D-03, D-09)
- ReaderLinkAction.OpenProfile (D-04)
- Exhaustive Profile when arms (READ-01)
- TtsText rewrite so speech uses @name
- Nip19Test, NostrLinkTest, NostrMentionsTest, ReaderLinksTest, UrlExtractorTest, TtsTextTest cases

## API coverage

No new external API. Relays are not queried for persist in this plan; decode only.

<output>
After completion, create `.planning/phases/05-resolve-nostr-profile-references/05-01-SUMMARY.md`
</output>
