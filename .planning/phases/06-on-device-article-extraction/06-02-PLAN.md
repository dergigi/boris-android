---
phase: 06-on-device-article-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/src/main/java/org/dergigi/boris/data/HtmlToMarkdown.kt
  - app/src/main/java/org/dergigi/boris/data/ArticleExtractor.kt
  - app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
  - app/src/test/java/org/dergigi/boris/data/HtmlToMarkdownTest.kt
  - app/src/test/java/org/dergigi/boris/data/ArticleExtractorTest.kt
  - app/src/test/java/org/dergigi/boris/data/ReaderRepositoryParseTest.kt
autonomous: true
requirements:
  - READ-04
  - READ-01
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-05: Extracted Markdown includes headings, lists, blockquotes, code, GFM tables, footnotes, title, obvious byline, body, and images."
    - "D-06: Image src stays origin https after convert. Relative src still resolves against the article URL."
    - "D-08: Converted Markdown shorter than MIN_ARTICLE_MARKDOWN_CHARS (500, same number as today's RSS teaser bar) is a failure, not a Ready teaser."
    - "D-11: Paywall teasers use the same 500-character rule. No separate paywall detector."
    - "Empty: a cookie-wall or thin teaser is not a hollow reader. parse markdown is null under the bar (D-08)."
    - "READ-04: rssContent still calls HtmlToMarkdown.convert(html) without ArticleExtractor. Existing RSS tests stay green."
    - "READ-01: No session check is added on fetch or convert."
  artifacts:
    - path: app/src/main/java/org/dergigi/boris/data/HtmlToMarkdown.kt
      provides: GFM tables, footnote pairs, ordered lists, byline prepend (D-05)
      exports: ["HtmlToMarkdown"]
    - path: app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
      provides: shared MIN_ARTICLE_MARKDOWN_CHARS = 500 for RSS and web extract (D-08, D-11)
      exports: ["MIN_ARTICLE_MARKDOWN_CHARS"]
  key_links:
    - from: app/src/main/java/org/dergigi/boris/data/HtmlToMarkdown.kt
      to: app/src/main/java/org/dergigi/boris/data/Footnotes.kt
      via: converter emits [^id] and [^id]: text so Footnotes.expand still renders
      pattern: "[^"
    - from: app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
      to: app/src/main/java/org/dergigi/boris/data/ArticleExtractor.kt
      via: after markdown() convert, reject length under 500
      pattern: MIN_ARTICLE_MARKDOWN_CHARS
  prohibitions:
    - statement: Do not bump versionCode, versionName, CHANGELOG, or publish Zapstore
      status: resolved
      verification: explicit
    - statement: Do not add flexmark, Remark, or a second Markdown converter
      status: resolved
      verification: explicit
    - statement: Do not add a ReadableContent.byline field
      status: resolved
      verification: explicit
    - statement: Do not invent a second empty-extract number
      status: resolved
      verification: explicit
    - statement: Do not run ArticleExtractor on RSS bodies
      status: resolved
      verification: explicit
---

<objective>
Raise extract quality to the locked Jina-like bar (D-05, D-06) and fail near-empty bodies including paywall teasers (D-08, D-11) using the existing 500-character RSS constant. READ-04 READ-01.
Purpose: 06-01 already fetches origin HTML and strips chrome. Without tables, footnotes, ordered lists, byline, and the shared 500-char fail, D-05 and D-08 are unmet.
Output: extended HtmlToMarkdown, shared MIN_ARTICLE_MARKDOWN_CHARS, JVM fixtures
</objective>

<assumption_delta_decision>
noun: empty-extract threshold
decision: new-assumption
rationale: Discretion locked to 500 Markdown characters, the same number as MIN_RSS_MARKDOWN_CHARS and Mozilla charThreshold. Rename to one shared constant. Do not invent a second bar.
</assumption_delta_decision>

<execution_context>
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-on-device-article-extraction/06-CONTEXT.md
@.planning/phases/06-on-device-article-extraction/06-RESEARCH.md
@.planning/phases/06-on-device-article-extraction/06-PATTERNS.md
@.planning/phases/06-on-device-article-extraction/06-01-SUMMARY.md
@app/src/main/java/org/dergigi/boris/data/HtmlToMarkdown.kt
@app/src/main/java/org/dergigi/boris/data/ArticleExtractor.kt
@app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
@app/src/main/java/org/dergigi/boris/data/Footnotes.kt
@app/src/test/java/org/dergigi/boris/data/HtmlToMarkdownTest.kt

<interfaces>
From 06-01: ArticleExtractor.article / markdown exist. HtmlToMarkdown.convert(element, baseUrl) forwards to convert(outerHtml, baseUrl). ReaderRepository.parse uses ArticleExtractor.markdown. rssContent still uses convert(html) and MIN_RSS_MARKDOWN_CHARS = 500.

From app/src/main/java/org/dergigi/boris/data/HtmlToMarkdown.kt: keep headings h1-h6, strong/em, blockquote, pre/code, links, images. Before the generic li to dash rule, convert ol li to 1. numbering. Add GFM tables from table / thead / tbody / tr / th / td (header row, separator, body rows). Add footnotes from sup a[href^=#] plus matching li[id] or .footnotes li as [^id] and [^id]: text so Footnotes.expand lights up. Byline if obvious: meta[name=author], [rel=author], [itemprop=author]. Prepend *byline* to the markdown. Do not add a ReadableContent field. Keep decode public.

From app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt: rename MIN_RSS_MARKDOWN_CHARS to MIN_ARTICLE_MARKDOWN_CHARS = 500 in companion (or a small shared helper). rssContent and web parse both use that one number. After ArticleExtractor.markdown, if the string is null or length is under 500, parse.markdown is null (D-08, D-11). Do not throw the D-13 no-article string this plan; 06-03 maps null markdown to that IOException. Do not edit Nostr arms.

From app/src/main/java/org/dergigi/boris/data/Footnotes.kt (do not edit): REFERENCE is [^id] not followed by colon. DEFINITION is ^[^id]: text. Emit that shape.

CONVENTIONS: org.dergigi.boris only, no KDoc, no TODO, no Log. JUnit 4 fixtures, no live network.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Extend HtmlToMarkdown for tables, footnotes, ordered lists, and byline</name>
  <files>app/src/main/java/org/dergigi/boris/data/HtmlToMarkdown.kt, app/src/test/java/org/dergigi/boris/data/HtmlToMarkdownTest.kt</files>
  <read_first>app/src/main/java/org/dergigi/boris/data/HtmlToMarkdown.kt, app/src/main/java/org/dergigi/boris/data/Footnotes.kt, app/src/test/java/org/dergigi/boris/data/HtmlToMarkdownTest.kt</read_first>
  <behavior>
    - Existing paragraph, link, image, heading, blockquote, and code tests stay identical
    - A 2x2 table becomes a GFM table with a separator row
    - A superscript footnote plus matching footnotes li becomes [^1] and [^1]: text
    - ol li items become 1. 2. not dashes
    - Obvious author meta prepends *Name* before the body
    - script tags still produce no script text
  </behavior>
  <action>
D-05: D-06: Keep every existing HtmlToMarkdownTest method. Extend the string pipeline; do not add a second converter library.

Add GFM tables, footnote pairs that Footnotes.expand already understands, and ol numbering before the generic dash-li rule. If author is obvious (meta name=author, rel=author, itemprop=author), prepend *byline* to the converted markdown. convert(element, baseUrl) stays the jsoup overload from 06-01. Images still go through articleUrl plus preferHttps (D-06). RSS keeps calling convert(html) without ArticleExtractor.

After verify passes, commit with: feat(06-02): emit tables, footnotes, ordered lists, and byline in article Markdown
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.data.HtmlToMarkdownTest --tests org.dergigi.boris.data.RssParserTest</automated>
  </verify>
  <acceptance_criteria>
    - HtmlToMarkdownTest.kt contains convertsParagraphsAndFormatting and a table fixture and [^1]
    - HtmlToMarkdown.kt converts ol and table and footnotes
    - ./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.data.HtmlToMarkdownTest --tests org.dergigi.boris.data.RssParserTest exits 0
  </acceptance_criteria>
  <done>D-05 Markdown extras exist. Existing converter and RSS tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Fail extracts shorter than 500 Markdown characters</name>
  <files>app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt, app/src/main/java/org/dergigi/boris/data/ArticleExtractor.kt, app/src/test/java/org/dergigi/boris/data/ReaderRepositoryParseTest.kt, app/src/test/java/org/dergigi/boris/data/ArticleExtractorTest.kt</files>
  <read_first>app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt, app/src/test/java/org/dergigi/boris/data/ReaderRepositoryParseTest.kt, app/src/test/java/org/dergigi/boris/data/ArticleExtractorTest.kt</read_first>
  <behavior>
    - HTML whose converted Markdown is under 500 characters yields parse.markdown null (D-08)
    - A paywall-teaser-sized body uses that same rule (D-11)
    - A 500-plus character article still returns markdown
    - rssContent still rejects feed bodies under the same 500 constant
    - parsesHtmlFallback fixture is lengthened so it is no longer a thin body
  </behavior>
  <action>
D-08: D-11: READ-04. Rename MIN_RSS_MARKDOWN_CHARS to MIN_ARTICLE_MARKDOWN_CHARS = 500 and use that single constant in rssContent and in parse after ArticleExtractor.markdown. If converted Markdown is null or length is under 500, set parse.markdown to null. Do not show a hollow Ready. Do not add a paywall detector. Do not throw D-13 copy here (06-03).

Update parsesHtmlFallback to a 500-plus character article (or assert null markdown if you keep the short Hi fixture as the thin case). Add an explicit thin-html test. ArticleExtractorTest already covers under-500 text; keep it aligned with the same 500 number.

After verify passes, commit with: feat(06-02): reject thin extracts with the shared 500-character bar
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.data.ReaderRepositoryParseTest --tests org.dergigi.boris.data.ArticleExtractorTest --tests org.dergigi.boris.data.RssParserTest</automated>
  </verify>
  <acceptance_criteria>
    - ReaderRepository.kt contains MIN_ARTICLE_MARKDOWN_CHARS
    - grep -vE '^[[:space:]]*(//|#|\*|/\*)' app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt | grep -c 'MIN_RSS_MARKDOWN_CHARS' is 0
    - ReaderRepositoryParseTest.kt covers a thin HTML body
    - ./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.data.ReaderRepositoryParseTest --tests org.dergigi.boris.data.ArticleExtractorTest --tests org.dergigi.boris.data.RssParserTest exits 0
  </acceptance_criteria>
  <done>Thin and paywall-teaser extracts are failures via one 500-character constant. RSS still uses that number.</done>
</task>

</tasks>

<threat_model asvs="1" block_on="high">

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extracted article node → HtmlToMarkdown | Untrusted HTML tags become Markdown tokens |
| Thin HTML → parse.markdown | A cookie wall or teaser must not become Ready |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | HtmlToMarkdown | high | mitigate | Keep stripping script/style/head. Do not add a renderer view. Tables and footnotes are Markdown text only (D-05, D-10). |
| T-06-05 | Spoofing | ArticleExtractor / parse | medium | mitigate | Chrome already stripped in 06-01. Thin leftover chrome fails the 500-char bar (D-08). |
| T-06-SC | Tampering | Gradle installs | high | mitigate | No new packages this plan. jsoup pin stays 1.23.1 from 06-01. |

</threat_model>

<verification>
- [ ] HtmlToMarkdownTest and RssParserTest pass
- [ ] Thin HTML parse.markdown is null
- [ ] One shared 500 constant
- [ ] No ReadableContent.byline field
</verification>

<success_criteria>
- D-05: tables, footnotes, ordered lists, byline, existing tags
- D-06: origin image URLs unchanged
- D-08 D-11: under 500 Markdown characters is a failure for web and RSS
- READ-04 READ-01: RSS/Nostr arms and logged-out read unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/06-on-device-article-extraction/06-02-SUMMARY.md`
</output>
