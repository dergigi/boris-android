---
phase: 06-on-device-article-extraction
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - app/src/main/java/org/dergigi/boris/data/HttpUserAgents.kt
  - app/src/main/java/org/dergigi/boris/data/OgMetaClient.kt
  - app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
  - app/src/main/java/org/dergigi/boris/data/ArticleCover.kt
  - app/src/main/java/org/dergigi/boris/data/PublishedTime.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/test/java/org/dergigi/boris/data/ArticleCoverTest.kt
  - app/src/test/java/org/dergigi/boris/data/PublishedTimeTest.kt
  - app/src/test/java/org/dergigi/boris/data/ReaderRepositoryParseTest.kt
autonomous: true
requirements:
  - READ-04
  - READ-01
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-09: First GET uses BORIS_UA. On 401, 403, or empty/thin extract, retry once with the existing Chrome Mobile UA from OgMetaClient. Then fail."
    - "D-12: Two reader errors only: fetch failed vs no article found."
    - "D-13: Copy is Could not reach this page. and Could not find an article on this page. Keys reader_error_unreachable and reader_error_no_article. No library or proxy names on screen."
    - "D-14: Open original stays on both errors when url is not blank. Same outlined button, same openOriginalArticle."
    - "D-15: Live origin fail plus OkHttp FORCE_CACHE hit on the origin URL serves that cached HTML through extract. Thin cache is no-article. Miss is unreachable."
    - "D-10: Retry is a second GET, not a renderer view. UA retry stays invisible (Loading until Ready or Error)."
    - "Empty: thin extract is Error no-article, not a hollow Ready (D-08 via 06-02, mapped here)."
    - "Loading: existing spinner. No Retrying label (06-UI-SPEC)."
    - "Error: one D-13 sentence, Try again, Open original if URL present. Same column for both kinds."
    - "Partial: live fail plus thick origin cache is Ready. No cached badge (D-15, 06-UI-SPEC)."
    - "Overflow / long-text: D-13 sentences wrap. Buttons stay Try again and Open original."
    - "Zero-one-many: exactly one error sentence. Never both."
    - "READ-01: No login sheet. Blank URL stays No URL to read."
    - "READ-04: Nostr and RSS error paths are unchanged."
  artifacts:
    - path: app/src/main/java/org/dergigi/boris/data/HttpUserAgents.kt
      provides: BORIS_UA and BROWSER_UA (D-09)
      exports: ["HttpUserAgents"]
    - path: app/src/main/res/values/strings.xml
      provides: reader_error_unreachable and reader_error_no_article (D-13)
      exports: ["reader_error_unreachable", "reader_error_no_article"]
    - path: app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
      provides: UA retry, two IOException messages, 2 MiB body cap, origin FORCE_CACHE (D-09, D-12, D-13, D-15)
      exports: ["ERROR_UNREACHABLE", "ERROR_NO_ARTICLE"]
  key_links:
    - from: app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
      to: app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt
      via: IOException message becomes ReaderUiState.Error.message
      pattern: Could not reach this page
    - from: app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt
      to: app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
      via: Error column shows state.message plus Try again and Open original (D-14)
      pattern: ReaderUiState.Error
    - from: app/src/main/java/org/dergigi/boris/data/OgMetaClient.kt
      to: app/src/main/java/org/dergigi/boris/data/HttpUserAgents.kt
      via: OG fetch uses the same BROWSER_UA string as the reader retry
      pattern: BROWSER_UA
  prohibitions:
    - statement: Do not bump versionCode, versionName, CHANGELOG, or publish Zapstore
      status: resolved
      verification: explicit
    - statement: Do not add a new Error sealed type, icon, snackbar, or third button
      status: resolved
      verification: explicit
    - statement: Do not name the proxy or extract libraries in UI copy
      status: resolved
      verification: explicit
    - statement: Do not construct a new OkHttpClient for the retry
      status: resolved
      verification: explicit
    - statement: Do not keep leftover Jina parse helpers as test-only fixtures
      status: resolved
      verification: explicit
---

<objective>
Finish blocked-page retry (D-09), the two locked error sentences (D-12, D-13, D-14), origin-cache fallback (D-15), a 2 MiB body cap, and delete leftover proxy parse helpers. READ-04 READ-01. 06-UI-SPEC approved: reuse the Error column; swap strings only.
Purpose: 06-01/06-02 already extract on-device. Users still see old fetch copy, there is no browser-UA retry, and Jina header helpers remain.
Output: HttpUserAgents, two string resources, origin retry/cache/error mapping, leftover helper deletion
</objective>

<assumption_delta_decision>
noun: User-Agent pair
decision: new-assumption
rationale: Discretion locked. First UA is Boris/${BuildConfig.VERSION_NAME} (Android; +https://github.com/dergigi/boris-android). Retry UA is the existing OgMetaClient Chrome Mobile string, moved to HttpUserAgents.BROWSER_UA.
</assumption_delta_decision>

<execution_context>
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-on-device-article-extraction/06-CONTEXT.md
@.planning/phases/06-on-device-article-extraction/06-RESEARCH.md
@.planning/phases/06-on-device-article-extraction/06-PATTERNS.md
@.planning/phases/06-on-device-article-extraction/06-UI-SPEC.md
@.planning/phases/06-on-device-article-extraction/06-01-SUMMARY.md
@.planning/phases/06-on-device-article-extraction/06-02-SUMMARY.md
@app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt
@app/src/main/java/org/dergigi/boris/data/OgMetaClient.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
@app/src/main/res/values/strings.xml
@app/src/main/java/org/dergigi/boris/data/ArticleCover.kt
@app/src/main/java/org/dergigi/boris/data/PublishedTime.kt

<interfaces>
From 06-02: parse.markdown is null when converted text is under MIN_ARTICLE_MARKDOWN_CHARS. rssContent and Nostr arms unchanged. Origin GET already uses the honest Boris UA.

From app/src/main/java/org/dergigi/boris/data/HttpUserAgents.kt (create):
object HttpUserAgents {
  const val BORIS_UA = "Boris/${org.dergigi.boris.BuildConfig.VERSION_NAME} (Android; +https://github.com/dergigi/boris-android)"
  const val BROWSER_UA = "Mozilla/5.0 (Linux; Android 14) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Mobile Safari/537.36"
}
Copy BROWSER_UA from OgMetaClient.kt lines 33-34 exactly. Point OgMetaClient.fetch at HttpUserAgents.BROWSER_UA. Do not copy OgMetaClient's private 8s client into the reader.

From app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt:
ERROR_UNREACHABLE = Could not reach this page.
ERROR_NO_ARTICLE = Could not find an article on this page.
Outcome table from 06-RESEARCH Pattern 2: network/timeout/non-2xx after retry → ERROR_UNREACHABLE unless origin FORCE_CACHE returns a body; 401/403 on first try → one rebuild with BROWSER_UA; 2xx not HTML (image/*, application/pdf, application/json, empty) → treat as empty extract (retry UA if first try, then ERROR_NO_ARTICLE); 2xx HTML under 500 chars → same; live fail plus origin cache → extract again (thin → ERROR_NO_ARTICLE, good → success). Cap the response body at 2 MiB before parse. Reuse defaultClient. Second GET for retry, not a new client. Keep max-age=300. Do not force-cache unsuccessful responses.

From app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt: keep ReaderUiState.Error(message, url). Map e.message. Change the null-message fallback to Could not reach this page. Keep No URL to read. for blank URL. Do not add a session check (READ-01). Do not add a new sealed variant.

From app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt: keep the Error Column, bodyLarge centered message, filled Try again, outlined Open original when state.url is not blank (D-14). Prefer stringResource(R.string.reader_open_original) like the overflow menu. Do not add icons, colors, type roles, cards, or a third button (06-UI-SPEC).

From app/src/main/res/values/strings.xml: add reader_error_unreachable and reader_error_no_article with the two D-13 sentences. Repository Kotlin constants match those sentences so JVM tests need no Resources.

From ArticleCover / PublishedTime: delete leftover proxy-header helpers. Keep stripLeadingImage, firstMarkdownImage, fromHtml, parse, label.

CONVENTIONS: org.dergigi.boris only, no KDoc, no TODO, no Log.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>UA retry, two errors, body cap, origin cache fallback</name>
  <reversibility rating="one-way">This phase locks HTTP plus HTML only; a renderer view would be its own phase (D-10).</reversibility>
  <files>app/src/main/java/org/dergigi/boris/data/HttpUserAgents.kt, app/src/main/java/org/dergigi/boris/data/OgMetaClient.kt, app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt</files>
  <read_first>app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt, app/src/main/java/org/dergigi/boris/data/OgMetaClient.kt, .planning/phases/06-on-device-article-extraction/06-RESEARCH.md</read_first>
  <action>
D-09: D-10: D-12: D-13: D-15: READ-04. Create HttpUserAgents with BORIS_UA and BROWSER_UA. OgMetaClient.fetch uses BROWSER_UA. Reader first GET uses BORIS_UA (already set in 06-01; point it at the shared constant).

On 401, 403, or empty/thin extract after the first GET, retry once with BROWSER_UA on the same origin URL and the same defaultClient. Then fail. Do not add a renderer view (D-10). Cap bodies at 2 MiB. Map outcomes with the two D-13 IOException messages only. Live fail plus origin FORCE_CACHE hit re-runs extract (D-15). Thin cached extract is no-article. No origin cache is unreachable. Do not probe leftover proxy cache keys (D-02 already origin-only). Do not leak status codes in messages.

After verify passes, commit with: feat(06-03): retry blocked pages once and map two reader errors
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - HttpUserAgents.kt contains BORIS_UA and BROWSER_UA
    - OgMetaClient.kt contains HttpUserAgents.BROWSER_UA
    - ReaderRepository.kt contains Could not reach this page. and Could not find an article on this page. and BROWSER_UA
    - grep -vE '^[[:space:]]*(//|#|\*|/\*)' app/src/main/java/org/dergigi/boris/data/ReaderRepository.kt | grep -c 'Failed to fetch readable content' is 0
    - ./gradlew :app:assembleDebug exits 0
  </acceptance_criteria>
  <done>First GET is honest Boris UA, one Chrome-UA retry, two IOException sentences, 2 MiB cap, origin FORCE_CACHE fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Error strings, Open original, and leftover helper deletion</name>
  <files>app/src/main/res/values/strings.xml, app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt, app/src/main/java/org/dergigi/boris/data/ArticleCover.kt, app/src/main/java/org/dergigi/boris/data/PublishedTime.kt, app/src/test/java/org/dergigi/boris/data/ArticleCoverTest.kt, app/src/test/java/org/dergigi/boris/data/PublishedTimeTest.kt, app/src/test/java/org/dergigi/boris/data/ReaderRepositoryParseTest.kt</files>
  <read_first>app/src/main/res/values/strings.xml, app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt, app/src/main/java/org/dergigi/boris/data/ArticleCover.kt, app/src/main/java/org/dergigi/boris/data/PublishedTime.kt, app/src/test/java/org/dergigi/boris/data/ArticleCoverTest.kt, app/src/test/java/org/dergigi/boris/data/PublishedTimeTest.kt</read_first>
  <behavior>
    - strings.xml has reader_error_unreachable and reader_error_no_article with the two D-13 sentences
    - ViewModel null-message fallback is Could not reach this page.
    - Blank URL remains No URL to read.
    - Error column still shows Try again and Open original when url is not blank (D-14)
    - ArticleCover keeps stripLeadingImage and firstMarkdownImage
    - PublishedTime keeps fromHtml
    - parse on thin HTML stays markdown null; fetch mapping is the no-article sentence
  </behavior>
  <action>
<!-- planner-discipline-allow: fromJinaHeader -->
<!-- planner-discipline-allow: imageFromJina -->
<!-- planner-discipline-allow: descriptionFromJina -->
<!-- planner-discipline-allow: Failed to load this article. -->
D-12: D-13: D-14: 06-UI-SPEC. Add the two string resources verbatim. ReaderViewModel shows e.message and uses Could not reach this page. when message is null. Keep No URL to read. ReaderScreen keeps the existing Error column. Use reader_open_original for the outlined button. Do not add chrome, colors, type roles, icons, snackbars, or a third button. Logged out stays the same (READ-01).

Delete leftover proxy-header helpers from ArticleCover and PublishedTime (imageFromJina, descriptionFromJina, fromJinaHeader, and their header regexes). Keep stripLeadingImage, firstMarkdownImage, fromHtml, parse, label. Delete the matching header-helper tests. Keep firstMarkdownImage and stripLeadingImage tests. Keep fromHtmlMeta. Do not keep remote-proxy-format fixtures.

After verify passes, commit with: feat(06-03): lock two reader errors and drop leftover proxy parse helpers
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.data.ArticleCoverTest --tests org.dergigi.boris.data.PublishedTimeTest --tests org.dergigi.boris.data.ReaderRepositoryParseTest --tests org.dergigi.boris.data.ArticleExtractorTest --tests org.dergigi.boris.data.HtmlToMarkdownTest</automated>
    <human-check>Logged out: paste a working https article and confirm Ready looks like today. Airplane mode on a never-cached URL shows Could not reach this page. plus Try again and Open original. A JS-shell or teaser page shows Could not find an article on this page. plus the same two buttons. TalkBack must not hear library or proxy names. Do not expect a cached badge.</human-check>
  </verify>
  <acceptance_criteria>
    - strings.xml contains reader_error_unreachable and reader_error_no_article and Could not reach this page. and Could not find an article on this page.
    - grep -v '^#' app/src/main/res/values/strings.xml | grep -ciE 'jina|jsoup|readability' is 0
    - ReaderViewModel.kt contains Could not reach this page.
    - grep -vE '^[[:space:]]*(//|#|\*|/\*)' app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt | grep -c 'Failed to load this article.' is 0
    - grep -vE '^[[:space:]]*(//|#|\*|/\*)' app/src/main/java/org/dergigi/boris/data/ArticleCover.kt | grep -c 'imageFromJina' is 0
    - grep -vE '^[[:space:]]*(//|#|\*|/\*)' app/src/main/java/org/dergigi/boris/data/PublishedTime.kt | grep -c 'fromJinaHeader' is 0
    - ReaderScreen.kt still contains openOriginal and Try again
    - ./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.data.ArticleCoverTest --tests org.dergigi.boris.data.PublishedTimeTest --tests org.dergigi.boris.data.ReaderRepositoryParseTest --tests org.dergigi.boris.data.ArticleExtractorTest --tests org.dergigi.boris.data.HtmlToMarkdownTest exits 0
  </acceptance_criteria>
  <done>Two locked error sentences, Open original on both, leftover proxy helpers gone, JVM tests green.</done>
</task>

</tasks>

<threat_model asvs="1" block_on="high">

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Origin HTTP status / body → error kind | Untrusted host chooses 401/403/empty vs HTML |
| Repository IOException → ReaderUiState.Error | Message is shown verbatim on screen |
| Cached origin HTML → extract | Disk cache is still untrusted HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | Cached HTML re-extract | high | mitigate | Cache path runs the same ArticleExtractor. No renderer view on retry (D-10). |
| T-06-04 | Denial of Service | ReaderRepository body read | medium | mitigate | 2 MiB cap before parse. |
| T-06-07 | Information Disclosure | User-Agent | low | accept | Honest Boris UA first (D-09). Browser UA only on the one retry. Acceptable for a reader. |
| T-06-08 | Information Disclosure | ReaderUiState.Error | medium | mitigate | Only the two D-13 sentences. No status codes, host internals, or library names (D-13). |
| T-06-SC | Tampering | Gradle installs | high | mitigate | No new packages this plan. |

</threat_model>

<verification>
- [ ] ./gradlew :app:assembleDebug exits 0
- [ ] Named unit tests pass
- [ ] strings.xml has the two keys and no library/proxy names
- [ ] Open original still renders when Error.url is not blank
- [ ] Leftover proxy-header helpers are gone
</verification>

<success_criteria>
- D-09: one honest GET, one Chrome-UA retry, then fail
- D-12 D-13 D-14: two sentences, Open original on both, no third error copy
- D-15: origin FORCE_CACHE fallback, no leftover proxy keys
- D-10 READ-01 READ-04: no renderer view, reading ungated, Nostr/RSS untouched
- 06-UI-SPEC: existing Error column, strings only
</success_criteria>

<output>
After completion, create `.planning/phases/06-on-device-article-extraction/06-03-SUMMARY.md`
</output>
