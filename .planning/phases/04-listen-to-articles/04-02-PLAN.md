---
phase: 04-listen-to-articles
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/src/main/java/org/dergigi/boris/ui/settings/SettingsScreen.kt
  - app/src/main/java/org/dergigi/boris/ui/settings/TtsSection.kt
autonomous: true
requirements:
  - TTS-01
  - READ-01
user_setup:
  - A device or emulator with a system TTS engine installed, for preview play
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Settings match the webapp TTS section (speed, language, preview sentence) and sync via existing NIP-78 keys (D-06, D-07, D-08, D-09, D-10, ROADMAP-4)
    - Speed presets are 0.8, 1, 1.2, 1.4, 1.6, 1.8, 2, 2.1, 2.4, 2.8, 3 with default 2.1; the cycle chip writes ttsDefaultSpeed (D-08)
    - Speaker language dropdown values are system, content, en-US, en-GB, zh, es, hi, ar, fr, pt, de, ja, ru stored as ttsLanguageMode and the two boolean companions (D-09, D-07)
    - Preview sentence is exactly TtsPreview.EXAMPLE_TEXT and play is a one-shot that leaves TtsPlayback.session null and does not open a mini player (D-10)
    - Follow along while listening checkbox defaults on and writes ttsFollowAlong (D-13)
    - If TTS is missing, every settings control stays visible plus error copy and Open settings (D-11)
    - Empty: preview box always contains the locked sentence
    - Error: engine or language failure keeps controls visible and shows Open settings via Settings.ACTION_TEXT_TO_SPEECH_SETTINGS
    - Overflow: language menu scrolls if the screen is short; preview wraps at 16sp / 1.5
    - Long-text: preview wraps; speed label is short such as 3x
    - Reading still works logged out; logged-out setting changes last for the process as today's font size does (READ-01)
  artifacts:
    - path: app/src/main/java/org/dergigi/boris/ui/settings/TtsSection.kt
      provides: Webapp-shaped Text-to-Speech block inside Reading
      exports: ["TtsSection"]
    - path: app/src/main/java/org/dergigi/boris/ui/settings/SettingsScreen.kt
      provides: Reading category order ReadingSection then TtsSection then ReadingPreview
  key_links:
    - from: app/src/main/java/org/dergigi/boris/ui/settings/TtsSection.kt
      to: app/src/main/java/org/dergigi/boris/ui/settings/SettingsViewModel.kt
      via: onUpdate -> SettingsViewModel.update so NIP-78 still publishes the blob
      pattern: SettingsViewModel.update
    - from: app/src/main/java/org/dergigi/boris/ui/settings/TtsSection.kt
      to: app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt
      via: preview() one-shot of EXAMPLE_TEXT leaving session null; speed cycle also calls setRate when a session is live
      pattern: TtsPlayback.preview
  prohibitions:
    - statement: Do not add a top-level Settings category for TTS; it lives under Reading
      status: resolved
      verification: explicit
    - statement: Do not invent new NIP-78 key names for speed or language
      status: resolved
      verification: explicit
    - statement: Do not put EXAMPLE_TEXT in strings.xml
      status: resolved
      verification: explicit
    - statement: Preview leaves TtsPlayback.session null and must not create mini-player state
      status: resolved
      verification: explicit
---

<objective>
Ship the Reading-category Text-to-Speech section so Android settings match the webapp: speed cycle, speaker language dropdown, locked preview paragraph with play, follow-along checkbox, NIP-78 writes on the existing keys. D-06 D-07 D-08 D-09 D-10 D-11 D-13. TTS-01 READ-01.
Purpose: Speed and language must round-trip with readwithboris.com; a second store would desync.
Output: TtsSection inside SettingsCategory.Reading
</objective>

<execution_context>
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-listen-to-articles/04-CONTEXT.md
@.planning/phases/04-listen-to-articles/04-RESEARCH.md
@.planning/phases/04-listen-to-articles/04-PATTERNS.md
@.planning/phases/04-listen-to-articles/04-UI-SPEC.md
@.planning/phases/04-listen-to-articles/04-01-PLAN.md
@app/src/main/java/org/dergigi/boris/ui/settings/SettingsScreen.kt
@app/src/main/java/org/dergigi/boris/ui/settings/ReadingSection.kt
@app/src/main/java/org/dergigi/boris/ui/settings/SettingsControls.kt
@app/src/main/java/org/dergigi/boris/ui/settings/ReadingPreview.kt
@app/src/main/java/org/dergigi/boris/ui/settings/SettingsViewModel.kt
@app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt
@app/src/main/java/org/dergigi/boris/tts/TtsSpeed.kt
@app/src/main/java/org/dergigi/boris/tts/TtsLanguage.kt
@app/src/main/java/org/dergigi/boris/tts/TtsPreview.kt
@/Users/gigi/Development/vibe/boris/src/components/Settings/TTSSettings.tsx

<interfaces>
From 04-01 (do not re-own these files unless a one-line call is missing):
TtsPlayback.preview(text), setRate(rate), session StateFlow. Preview leaves session null.
TtsSpeed.cycle / SPEED_OPTIONS / DEFAULT
TtsLanguage.MODES
TtsPreview.EXAMPLE_TEXT
UserSettings.ttsDefaultSpeed, ttsLanguageMode, ttsUseSystemLanguage, ttsDetectContentLanguage, ttsFollowAlong
withDouble / withString / withBoolean

From app/src/main/java/org/dergigi/boris/ui/settings/SettingsViewModel.kt (do not edit):
fun update(transform: (UserSettings) -> UserSettings)
requestSave no-ops when SessionStore.load is null (same as font size; do not add a TTS-only store).

From app/src/main/java/org/dergigi/boris/ui/settings/SettingsScreen.kt:
SettingsCategory.Reading currently composes ReadingSection then ReadingPreview.
Insert TtsSection between them. Parent already spacedBy 24.dp (lg).

From app/src/main/java/org/dergigi/boris/ui/settings/ReadingSection.kt:
Copy FontDropdown: ExposedDropdownMenuBox + read-only OutlinedTextField, RoundedCornerShape(8.dp).
SettingRow / SettingCheckbox live in SettingsControls.kt.

From /Users/gigi/Development/vibe/boris/src/components/Settings/TTSSettings.tsx:
Language write sets ttsLanguageMode, ttsUseSystemLanguage (value === system), ttsDetectContentLanguage (value === content).
EXAMPLE_TEXT is the D-10 sentence. Do not rewrite it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Add TtsSection matching the webapp TTS settings</name>
  <precondition>04-01 artifacts exist: TtsPlayback.preview, TtsSpeed, TtsLanguage, TtsPreview.EXAMPLE_TEXT, UserSettings.ttsDefaultSpeed and ttsFollowAlong, strings tts_section_title through tts_lang_ru</precondition>
  <files>app/src/main/java/org/dergigi/boris/ui/settings/TtsSection.kt, app/src/main/java/org/dergigi/boris/ui/settings/SettingsScreen.kt</files>
  <read_first>.planning/phases/04-listen-to-articles/04-UI-SPEC.md, app/src/main/java/org/dergigi/boris/ui/settings/ReadingSection.kt, app/src/main/java/org/dergigi/boris/ui/settings/SettingsControls.kt, app/src/main/java/org/dergigi/boris/ui/settings/ReadingPreview.kt, app/src/main/java/org/dergigi/boris/ui/settings/SettingsScreen.kt, app/src/main/java/org/dergigi/boris/tts/TtsPreview.kt, app/src/main/java/org/dergigi/boris/tts/TtsSpeed.kt, /Users/gigi/Development/vibe/boris/src/components/Settings/TTSSettings.tsx</read_first>
  <action>
Add TtsSection inside SettingsCategory.Reading, after ReadingSection and before ReadingPreview. Not a new top-level category. Not under Media (D-06). Match 04-UI-SPEC surface 3 and TTSSettings.tsx.

Order:
1. Heading Text-to-Speech (titleMedium, onBackground) using tts_section_title.
2. SettingRow Default Playback Speed: speed cycle chip, Icons.Filled.Speed 18dp plus {rate}x, contentDescription Cycle speed, min 48dp touch, 40dp chip height like SettingChipSize. Cycle via TtsSpeed.cycle then onUpdate(settings.withDouble("ttsDefaultSpeed", next)) (D-08). If TtsPlayback session is playing, also TtsPlayback.setRate(next.toFloat()) so the live utterance restarts at the new rate.
3. SettingRow Speaker language: ExposedDropdownMenuBox like FontDropdown, width fills the control slot, 8.dp corners. Values and labels verbatim from UI-SPEC (system System Language, content Content (auto-detect), divider, then the ten locales). On select, write three keys (D-07, D-09): withString ttsLanguageMode value, withBoolean ttsUseSystemLanguage (value == system), withBoolean ttsDetectContentLanguage (value == content). If a session is live, apply the new locale on the engine (setLanguage + QUEUE_FLUSH current paragraph) through TtsPlayback, not by constructing a second TextToSpeech.
4. Preview box: 12.dp padding, 8.dp corners, surfaceVariant fill, 1.dp outline. Text is TtsPreview.EXAMPLE_TEXT exactly (D-10), 16sp Regular line-height 1.5, onBackground, wrap, never truncate. Trailing IconButton PlayArrow/Pause 48dp; contentDescription Listen to article / Pause playback. Play calls TtsPlayback.preview(TtsPreview.EXAMPLE_TEXT). One-shot only: leave TtsPlayback.session null for the whole utterance; no mini-player visibility. If an article is speaking, preview stops it (one engine). Pause on the preview stops that utterance.
5. SettingCheckbox Follow along while listening, checked = settings.ttsFollowAlong, onCheckedChange writes withBoolean ttsFollowAlong (D-13). Default true from 04-01.

Use existing string resources from 04-01. Do not put EXAMPLE_TEXT in strings.xml. Sans on all TTS chrome; do not apply Source Serif to these controls. Weights 400 and 500 only. Collect TtsPlayback only for preview icon state and live rate apply.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - TtsSection.kt contains TtsSection and ttsDefaultSpeed and ttsLanguageMode and ttsFollowAlong and EXAMPLE_TEXT
    - TtsSection.kt contains ttsUseSystemLanguage and ttsDetectContentLanguage
    - SettingsScreen.kt contains TtsSection
    - grep -n TtsSection app/src/main/java/org/dergigi/boris/ui/settings/SettingsScreen.kt | grep -c TtsSection is at least 1
    - TtsPreview.kt still contains Boris aims to be a calm reader app with clean typography
  </acceptance_criteria>
  <done>Reading settings shows a Text-to-Speech block whose speed, language, preview sentence, and follow-along checkbox match the webapp contract and write the existing NIP-78 keys.</done>
</task>

<task type="auto">
  <name>Keep TTS controls visible on engine failure and deep-link system settings</name>
  <files>app/src/main/java/org/dergigi/boris/ui/settings/TtsSection.kt</files>
  <read_first>app/src/main/java/org/dergigi/boris/ui/settings/TtsSection.kt, app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt, .planning/phases/04-listen-to-articles/04-UI-SPEC.md</read_first>
  <action>
D-11 in the settings form (UI error consideration). When TtsPlayback.session errorMessage is engine-missing or language-missing, keep every TtsSection control visible (heading, speed, language, preview, follow-along). Under the preview, show tts_error_engine or tts_error_language as body text plus a text button tts_open_settings that starts Intent(Settings.ACTION_TEXT_TO_SPEECH_SETTINGS) with FLAG_ACTIVITY_NEW_TASK. If that activity is missing, fall back to ACTION_INSTALL_TTS_DATA then generic Settings. Do not hide Listen in the reader (already 04-01). Do not change ReaderUiState. Logged-out users can still open Settings and play preview; requestSave remaining a no-op without a session is accepted (pitfall 9).
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug :app:testDebugUnitTest --tests org.dergigi.boris.tts.TtsSpeedTest --tests org.dergigi.boris.tts.TtsLanguageTest --tests org.dergigi.boris.data.UserSettingsTest</automated>
  </verify>
  <acceptance_criteria>
    - TtsSection.kt contains ACTION_TEXT_TO_SPEECH_SETTINGS or tts_open_settings
    - TtsSection.kt still contains TtsSection and EXAMPLE_TEXT
    - ./gradlew :app:assembleDebug exits 0
  </acceptance_criteria>
  <done>Missing engine or language cannot hide TTS settings controls. Open settings reaches system TTS settings. Reading still works.</done>
</task>

</tasks>

<threat_model asvs="1" block_on="high">

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → UserSettings JSON → NIP-78 | Speed/language/follow-along join the existing settings blob |
| Preview play → TextToSpeech | Locked sentence only; must not enqueue article body from Settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Tampering | TtsSection language write | medium | mitigate | Write only the three locked keys (ttsLanguageMode, ttsUseSystemLanguage, ttsDetectContentLanguage); do not invent a second blob |
| T-04-09 | Information Disclosure | TtsPlayback.preview | high | mitigate | Preview speaks TtsPreview.EXAMPLE_TEXT only; do not pass ReadableContent.body into preview |
| T-04-10 | Elevation of Privilege | Open settings button | low | accept | Implicit ACTION_TEXT_TO_SPEECH_SETTINGS is a system screen; no exported component added |
| T-04-04 | Information Disclosure | TtsSection | high | mitigate | No android.util.Log; do not Toast the preview sentence |
| T-04-SC | Tampering | Gradle installs | high | mitigate | No new packages |

</threat_model>

<verification>
- [ ] ./gradlew :app:assembleDebug exits 0
- [ ] SettingsScreen Reading composes TtsSection between ReadingSection and ReadingPreview
- [ ] Language onSelect writes all three webapp keys
- [ ] TtsPreview.EXAMPLE_TEXT is unchanged
</verification>

<success_criteria>
- D-06 D-08 D-09 D-10 D-13: Settings UI matches the webapp TTS section, including the locked preview sentence
- D-07: Speed and language persist on the existing NIP-78 user-settings JSON
- D-11: Missing TTS cannot hide the section; Open settings is present
- READ-01: Settings sit on their existing route; reading stays ungated
</success_criteria>

## Artifacts this phase produces

- TtsSection under SettingsCategory.Reading (D-06, D-08, D-09, D-10, D-13)
- NIP-78 writes for ttsDefaultSpeed / ttsLanguageMode / companion booleans / ttsFollowAlong (D-07)
- D-11 Open settings deep link from the TTS section

## API coverage

No external API integration: on-device android.speech.tts.TextToSpeech and Android MediaSession only.

<output>
After completion, create `.planning/phases/04-listen-to-articles/04-02-SUMMARY.md`
</output>
