---
phase: 04-listen-to-articles
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/src/main/java/org/dergigi/boris/ui/BorisApp.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/ArticleFind.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/HighlightMarks.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt
  - app/src/main/java/org/dergigi/boris/ui/shell/TtsMiniPlayer.kt
  - app/src/main/java/org/dergigi/boris/ui/theme/Color.kt
autonomous: false
requirements:
  - TTS-01
  - READ-01
user_setup:
  - A device or emulator with a system TTS engine installed, for follow-along and mini-player UAT
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Follow-along highlights and auto-scrolls the spoken paragraph, on by default, disableable (D-12, D-13, ROADMAP-5)
    - If the user scrolls the article while it is speaking, auto-scroll stops until play or skip; speech continues; the mark stays (D-15)
    - When listening and the user is not on that article, a slim in-app mini player shows title plus play/pause/skip; title tap opens that article. Show only when session.url is a non-blank article URL (D-17)
    - Speed cycle is on the in-app player as well as in Settings, same presets, same ttsDefaultSpeed key (D-18)
    - Spoken fill is SpokenMark #2DD4BF at 0.38f, filled never underline, distinct from Find and NIP-84 colors; paints even when showHighlights is false
    - Populated: speaking shows Pause in the top bar; spoken fill plus auto-scroll when follow-along is on and not user-paused; mini player when session.url is a non-blank article URL and the current route is not that article
    - Partial: user scroll pauses auto-scroll only
    - Overflow: mini-player title and reader top-bar title are 1 line with end ellipsis
    - Zero-one-many: end of article dismisses the session and mini player
    - Long-text: mini-player title ellipsizes; tap still opens the article
    - Listening works logged out; reading still works if TTS is missing (TTS-01, READ-01)
  artifacts:
    - path: app/src/main/java/org/dergigi/boris/ui/theme/Color.kt
      provides: SpokenMark token
    - path: app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt
      provides: PaintedHighlight.spoken flag
    - path: app/src/main/java/org/dergigi/boris/ui/reader/HighlightMarks.kt
      provides: Spoken fill pass at FindMarkAlpha
    - path: app/src/main/java/org/dergigi/boris/ui/shell/TtsMiniPlayer.kt
      provides: Slim 56dp player chrome (D-17, D-18)
      exports: ["TtsMiniPlayer"]
    - path: app/src/main/java/org/dergigi/boris/ui/BorisApp.kt
      provides: Mini player above BorisBottomBar when the route is not the speaking article
  key_links:
    - from: app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
      to: app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt
      via: Append spoken PaintedHighlight when session url matches and follow-along is on and not user-paused; HighlightJump auto-scroll with followAlongScrolling guard (D-12, D-15)
      pattern: spoken mark
    - from: app/src/main/java/org/dergigi/boris/ui/BorisApp.kt
      to: app/src/main/java/org/dergigi/boris/ui/shell/TtsMiniPlayer.kt
      via: bottomBar column player then nav when a tab is showing; overlay on a different reader; only if session.url is a non-blank article URL
      pattern: TtsMiniPlayer
    - from: app/src/main/java/org/dergigi/boris/ui/shell/TtsMiniPlayer.kt
      to: app/src/main/java/org/dergigi/boris/ui/settings/SettingsViewModel.kt
      via: Speed chip writes ttsDefaultSpeed through SettingsViewModel.update and TtsPlayback.setRate
      pattern: TtsSpeed.cycle
  prohibitions:
    - statement: Do not publish a NIP-84 highlight for follow-along
      status: resolved
      verification: explicit
    - statement: Do not reuse FindMark or HighlightMine colors for speech
      status: resolved
      verification: explicit
    - statement: Do not show the mini player on the speaking article
      status: resolved
      verification: explicit
    - statement: Do not add a progress bar, artwork, author line, or stop button on the in-app bar
      status: resolved
      verification: explicit
---

<objective>
Add follow-along (teal spoken fill + auto-scroll with D-15 user-scroll pause) and the slim in-app mini player with speed cycle so listening feels like a podcast while the user stays in a reader. D-12 D-13 D-15 D-17 D-18. TTS-01 READ-01.
Purpose: Engine-only listen from 04-01 has no on-page tracking and no chrome off the article.
Output: SpokenMark paint, HighlightJump follow-along, TtsMiniPlayer in BorisApp
</objective>

<execution_context>
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-listen-to-articles/04-CONTEXT.md
@.planning/phases/04-listen-to-articles/04-RESEARCH.md
@.planning/phases/04-listen-to-articles/04-PATTERNS.md
@.planning/phases/04-listen-to-articles/04-UI-SPEC.md
@.planning/phases/04-listen-to-articles/04-01-PLAN.md
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt
@app/src/main/java/org/dergigi/boris/ui/reader/HighlightMarks.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ArticleFind.kt
@app/src/main/java/org/dergigi/boris/ui/reader/HighlightJump.kt
@app/src/main/java/org/dergigi/boris/ui/theme/Color.kt
@app/src/main/java/org/dergigi/boris/ui/BorisApp.kt
@app/src/main/java/org/dergigi/boris/ui/shell/BorisBottomBar.kt
@app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt

<interfaces>
From 04-01:
TtsPlayback.session: url, title, paragraphs, index, playing, paused, followAlongEnabled, followAlongPaused, rate. Preview leaves session null. Article listen always has a non-blank url.
TtsPlayback.pause/resume/skip/stop/setRate/setFollowAlongPaused
TtsSpeed.cycle
SettingsSync.settings.ttsFollowAlong
Routes.reader(url)

From app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt:
data class PaintedHighlight(..., find: Boolean = false, ignoreCase: Boolean = false)
Add spoken: Boolean = false. Do not add TTS engine fields to the ViewModel.

From app/src/main/java/org/dergigi/boris/ui/reader/ArticleFind.kt:
HIGHLIGHT_ID = find; painted(query) sets find=true.
Add SPOKEN_ID = spoken and a paintedSpoken(paragraph) factory in this file or a tiny sibling object. Quote is the current paragraph display text.

From app/src/main/java/org/dergigi/boris/ui/reader/HighlightMarks.kt:
paint({ it.find }, findColor, asUnderline = false, alpha = FindMarkAlpha)
Add a spoken pass first or after find: spoken == true, SpokenMark, asUnderline = false, alpha = FindMarkAlpha (0.38f). Rect inset stays 5.dp / 3.dp.

From app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt:
painted = HighlightJump.withFocus(highlights.visibleFor(settings), ...) + listOfNotNull(ArticleFind.painted(findQuery))
Append spoken mark outside visibleFor (same as Find) so showHighlights false still paints it (D-12).
Find jump LaunchedEffect uses HighlightJump.scrollTarget / awaitStop. Reuse that for follow-along.
snapshotFlow { scrollState.value } saves reading position; ignore deltas while followAlongScrolling is true so D-15 does not treat our animateScrollTo as user scroll.

From app/src/main/java/org/dergigi/boris/ui/BorisApp.kt:
bottomBar currently only BorisBottomBar when selectedTab != null. Reader has no tab bar.
TtsMiniPlayer: tab destinations sit above BorisBottomBar in the same bottomBar column; different-article reader overlays the bottom above gesture insets; speaking article hides the mini player. Compose only when session.url is a non-blank article URL so Settings preview cannot leak the bar.

From app/src/main/java/org/dergigi/boris/ui/theme/Color.kt:
Add val SpokenMark = Color(0xFF2DD4BF). Do not change FindMark or HighlightMine.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Spoken-paragraph follow-along mark and auto-scroll</name>
  <precondition>04-01 TtsPlayback.session exposes url, index, paragraphs, playing, followAlongPaused, and setFollowAlongPaused; UserSettings.ttsFollowAlong exists</precondition>
  <files>app/src/main/java/org/dergigi/boris/ui/theme/Color.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt, app/src/main/java/org/dergigi/boris/ui/reader/HighlightMarks.kt, app/src/main/java/org/dergigi/boris/ui/reader/ArticleFind.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt</files>
  <read_first>.planning/phases/04-listen-to-articles/04-UI-SPEC.md, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt, app/src/main/java/org/dergigi/boris/ui/reader/HighlightMarks.kt, app/src/main/java/org/dergigi/boris/ui/reader/ArticleFind.kt, app/src/main/java/org/dergigi/boris/ui/reader/HighlightJump.kt, app/src/main/java/org/dergigi/boris/ui/theme/Color.kt, app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt</read_first>
  <action>
Implement follow-along as a transient mark, not a signed highlight event (D-12, D-13, D-15). Do not call the highlight publisher or the signer from this work. Do not reuse FindMark #93C5FD or HighlightMine #FDE047.

1. Color.kt: SpokenMark = Color(0xFF2DD4BF).

2. PaintedHighlight: add spoken: Boolean = false. ArticleFind or sibling: SPOKEN_ID = spoken; paintedSpoken(paragraph) returns id spoken, quote = paragraph, mine false, spoken true.

3. HighlightMarks.drawHighlightMarks: paint spoken spans with SpokenMark, asUnderline false, alpha FindMarkAlpha. Keep find / mine / friends / other passes. Spoken fill is instant on onStart (index change); clear when session is null. No pulse.

4. ReaderScreen of the speaking url: if settings.ttsFollowAlong (D-13 default true) and session matches this url, append paintedSpoken(paragraphs[index]) beside ArticleFind.painted, outside visibleFor. When the checkbox is off, no mark and no auto-scroll. Follow-alongPaused (D-15): keep the mark, skip auto-scroll, keep speaking.

5. Auto-scroll: when follow-along is on, not paused, and index changes, reuse HighlightJump.scrollTarget + awaitStop / animateScrollTo like the find jump. Set followAlongScrolling true around that motion so snapshotFlow { scrollState.value } does not treat it as user scroll and does not call setFollowAlongPaused. Any other scrollState change while playing this url calls TtsPlayback.setFollowAlongPaused(true). Play (resume) or skip sets followAlongPaused false so auto-scroll resumes. Manual scroll must not stop speech (D-15).

6. Pause: mark remains on the current paragraph. Stop or end of article: mark clears. Do not paint spoken fill on Home, Library, Feed, Settings, or a different article.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - Color.kt contains SpokenMark and 0xFF2DD4BF
    - ReaderViewModel.kt contains spoken: Boolean
    - HighlightMarks.kt contains SpokenMark or it.spoken
    - ArticleFind.kt contains spoken or SPOKEN_ID
    - ReaderScreen.kt contains setFollowAlongPaused or followAlong
    - grep -vE '^[[:space:]]*(//|#|\*)' app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt app/src/main/java/org/dergigi/boris/ui/reader/HighlightMarks.kt app/src/main/java/org/dergigi/boris/ui/reader/ArticleFind.kt | grep -c Nip84 is 0
  </acceptance_criteria>
  <done>While speaking with follow-along on, the current paragraph is a teal fill and the reader auto-scrolls to it. User scroll pauses auto-scroll only. Checkbox off removes mark and auto-scroll. No highlight event is published.</done>
</task>

<task type="auto">
  <name>Slim in-app mini player with speed cycle</name>
  <files>app/src/main/java/org/dergigi/boris/ui/shell/TtsMiniPlayer.kt, app/src/main/java/org/dergigi/boris/ui/BorisApp.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt</files>
  <read_first>.planning/phases/04-listen-to-articles/04-UI-SPEC.md, app/src/main/java/org/dergigi/boris/ui/BorisApp.kt, app/src/main/java/org/dergigi/boris/ui/shell/BorisBottomBar.kt, app/src/main/java/org/dergigi/boris/ui/settings/SettingsViewModel.kt, app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt, app/src/main/java/org/dergigi/boris/tts/TtsSpeed.kt</read_first>
  <action>
Add TtsMiniPlayer (D-17, D-18) per 04-UI-SPEC surface 2. Collect TtsPlayback. Show TtsMiniPlayer only when session.url is a non-blank article URL and the current route is not Routes.reader of that url (top bar owns play/pause there). Preview leaves session null, so Settings preview cannot show the mini player. Hide when session is null or url is blank.

Placement: tab destinations (selectedTab != null) — 56.dp row directly above BorisBottomBar inside the Scaffold bottomBar column (player then nav). Reader of a different article (no tab bar) — overlay the bottom of that reader above system gesture insets. 200ms fade appear/disappear, no slide, no drop shadow. Surface / surfaceVariant plus 1.dp top outline.

Layout: title (labelLarge 14sp Medium, 1 line ellipsis, weight 1f, contentDescription Open article, fallback string tts_mini_player_fallback) then speed chip {rate}x (outlined 8.dp, Cycle speed) then SkipPrevious, PlayArrow or Pause, SkipNext. All transport buttons 48.dp. ContentDescriptions Previous paragraph / Pause playback or Resume playback / Next paragraph. Title tap navigates Routes.reader(session.url). Speed chip: TtsSpeed.cycle then SettingsViewModel.update { withDouble ttsDefaultSpeed } and TtsPlayback.setRate (D-18). Do not show a progress bar, artwork, author line, or in-app Stop (Stop stays on the system notification). RTL: control row mirrors; title still ellipsizes at the end.

On the speaking article, do not also show the mini player. End of article (session null) dismisses it (zero-one-many).
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug :app:testDebugUnitTest --tests org.dergigi.boris.tts.TtsSpeedTest --tests org.dergigi.boris.data.UserSettingsTest</automated>
  </verify>
  <acceptance_criteria>
    - TtsMiniPlayer.kt contains TtsMiniPlayer and SkipPrevious and ttsDefaultSpeed
    - TtsMiniPlayer.kt or BorisApp.kt contains isNotBlank
    - BorisApp.kt contains TtsMiniPlayer
    - TtsMiniPlayer.kt contains 56
    - ./gradlew :app:assembleDebug exits 0
  </acceptance_criteria>
  <done>Off-article listening shows a slim player with title, speed cycle, and paragraph skip. Title opens the article. The speaking article keeps using the top-bar control only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Device UAT for listen chrome</name>
  <read_first>.planning/phases/04-listen-to-articles/04-UI-SPEC.md, .planning/ROADMAP.md</read_first>
  <what-built>On-device article TTS with background MediaSession playback (04-01), webapp-matched Reading TTS settings (04-02), follow-along spoken fill, and the in-app mini player (this plan).</what-built>
  <how-to-verify>
Install the debug APK. Logged out, paste a URL and read (READ-01). On a Ready article tap Listen; confirm speech, Pause, Resume, lock-screen skip paragraph, and speech continuing on Home. Open a second article without tapping Listen; the first keeps speaking and the mini player is visible; tap the title to return. Tap Listen on the second article; the first stops. Confirm teal follow-along fill and auto-scroll; scroll by hand and confirm speech continues without yanking; tap skip and auto-scroll resumes. Turn off Follow along while listening; fill and auto-scroll stop. Cycle speed in Settings and on the mini player; both show the same {rate}x and write ttsDefaultSpeed. Preview play speaks the locked Boris aims to be a calm reader app sentence only and does not show the mini player. Volume keys while speaking change volume, not scroll. If the engine is missing, Listen stays visible, Open settings reaches system TTS settings, and reading still works.
  </how-to-verify>
  <resume-signal>Type approved or describe issues</resume-signal>
  <acceptance_criteria>
    - Human confirmed ROADMAP success criteria 1-6 on a device with a TTS engine, or listed concrete failures
  </acceptance_criteria>
  <done>Human approved listen UAT, or filed the gaps for a follow-up plan.</done>
</task>

</tasks>

<threat_model asvs="1" block_on="high">

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TtsPlayback paragraph text → reader paint | Spoken quote is displayed as Compose Text, not executed |
| Mini player → navigation | Title tap must open only Routes.reader(session.url) |
| Follow-along → highlights pipeline | Must not enter the NIP-84 sign/publish path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | ReaderScreen follow-along | high | mitigate | Transient PaintedHighlight.spoken only; no Nip84, no sign_event, no EventPublisher |
| T-04-11 | Spoofing | TtsMiniPlayer title tap | medium | mitigate | Navigate Routes.reader with the session url already being spoken; do not take clipboard or intent extras |
| T-04-02 | Information Disclosure | TtsMiniPlayer | medium | mitigate | Title only, ellipsized; no paragraph body on the bar |
| T-04-05 | Denial of Service | ReaderScreen | high | mitigate | Follow-along and mini player are overlay chrome; ReaderUiState load/error/ready unchanged (READ-01) |
| T-04-SC | Tampering | Gradle installs | high | mitigate | No new packages |

</threat_model>

<verification>
- [ ] ./gradlew :app:assembleDebug exits 0
- [ ] SpokenMark exists and HighlightMarks paints it.spoken as a fill
- [ ] ReaderScreen follow-along path has no Nip84 symbol
- [ ] BorisApp composes TtsMiniPlayer
</verification>

<success_criteria>
- D-12 D-13 D-15: Follow-along fill and auto-scroll, default on, user-scroll pauses auto-scroll only
- D-17 D-18: Mini player off-article with title, transport, and speed cycle on ttsDefaultSpeed
- TTS-01 READ-01: Listen still works logged out; reading still works if TTS is missing
- UI considerations populated, partial, overflow, zero-one-many, long-text hold on follow-along and mini player
</success_criteria>

## Artifacts this phase produces

- SpokenMark color + PaintedHighlight.spoken + spoken paint pass (D-12)
- Follow-along auto-scroll with followAlongScrolling / followAlongPaused (D-15)
- ttsFollowAlong honored in the reader (D-13)
- TtsMiniPlayer + BorisApp wiring (D-17, D-18)

## API coverage

No external API integration: on-device android.speech.tts.TextToSpeech and Android MediaSession only.

<output>
After completion, create `.planning/phases/04-listen-to-articles/04-03-SUMMARY.md`
</output>
