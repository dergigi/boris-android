---
phase: 01-amber-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/AndroidManifest.xml
  - app/src/main/res/xml/data_extraction_rules.xml
  - app/src/main/res/xml/full_backup_content.xml
  - app/src/main/res/values/strings.xml
  - app/src/main/java/org/dergigi/boris/nostr/RemoteSignerBridge.kt
  - app/src/main/java/org/dergigi/boris/nostr/Nip19.kt
  - app/src/main/java/org/dergigi/boris/nostr/SignerResult.kt
  - app/src/main/java/org/dergigi/boris/data/Session.kt
  - app/src/main/java/org/dergigi/boris/data/SessionStore.kt
  - app/src/main/java/org/dergigi/boris/ui/auth/AuthUiState.kt
  - app/src/main/java/org/dergigi/boris/ui/auth/AuthViewModel.kt
  - app/src/main/java/org/dergigi/boris/ui/auth/AuthBar.kt
  - app/src/main/java/org/dergigi/boris/ui/home/HomeScreen.kt
  - app/src/main/java/org/dergigi/boris/ui/BorisApp.kt
  - app/src/test/java/org/dergigi/boris/nostr/Nip19Test.kt
  - app/src/test/java/org/dergigi/boris/nostr/SignerResultTest.kt
  - app/src/test/java/org/dergigi/boris/data/SessionStoreTest.kt
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - READ-01
user_setup:
  - Amber (com.greenart7c3.nostrsigner) installed on a device or emulator for UAT of Connect / restart / sign out
  - A second run with Amber uninstalled to UAT AUTH-04
must_haves:
  truths:
    - User can tap Connect, approve in Amber, and see their npub on Home
    - After killing and reopening Boris, the same npub is still shown and Amber does not open again
    - User can sign out; Home no longer shows the npub; a later Connect is a fresh Amber prompt
    - With Amber uninstalled, Home says Amber is missing and points at Zapstore first, with F-Droid and GitHub as secondary options; Connect does not fail silently
    - User can still paste, share, or open a URL and read while logged out
  artifacts:
    - app/src/main/java/org/dergigi/boris/nostr/RemoteSignerBridge.kt
    - app/src/main/java/org/dergigi/boris/nostr/Nip19.kt
    - app/src/main/java/org/dergigi/boris/data/SessionStore.kt
    - app/src/main/java/org/dergigi/boris/ui/auth/AuthBar.kt
    - app/src/main/AndroidManifest.xml
  key_links:
    - HomeScreen owns rememberLauncherForActivityResult and calls RemoteSignerBridge.buildGetPublicKeyIntent
    - SessionStore persists pubkey_hex and signer_package in SharedPreferences boris_session
    - AuthBar shows npub from stored hex via Nip19; missing-Amber links Zapstore first
  prohibitions:
    - statement: Boris must not show an nsec paste field or persist a private key
      status: resolved
      verification: explicit
    - statement: Boris must not call get_public_key while a Session pair is stored
      status: resolved
      verification: explicit
    - statement: The first get_public_key Intent must not set package
      status: resolved
      verification: explicit
    - statement: Missing-Amber primary install link must not be the Play Store
      status: resolved
      verification: explicit
---

<objective>
Ship optional Amber identity on Home: Connect via NIP-55 get_public_key, persist hex + signer package, show npub, sign out, and a Zapstore-first missing-signer path. Reading stays ungated.

Purpose: AUTH-01 AUTH-02 AUTH-03 AUTH-04 READ-01 (D-01 through D-10)
Output: Thin login helper under org.dergigi.boris, Home AuthBar, JVM tests, manifest queries + backup exclude
</objective>

<execution_context>
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-amber-login/01-CONTEXT.md
@.planning/phases/01-amber-login/01-RESEARCH.md
@.planning/phases/01-amber-login/01-PATTERNS.md
@.planning/research/PITFALLS.md
@app/src/main/java/org/dergigi/boris/ui/home/HomeScreen.kt
@app/src/main/java/org/dergigi/boris/ui/BorisApp.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt
@app/src/main/AndroidManifest.xml
</context>

<threat_model asvs="1" block_on="high">
  <trust_boundaries>
    <boundary>Boris process vs Amber (NIP-55 intent extras)</boundary>
    <boundary>SharedPreferences boris_session vs Android backup</boundary>
    <boundary>Logcat vs pubkey/result extras</boundary>
  </trust_boundaries>
  <threats>
    <threat id="T-01" category="Information Disclosure" component="UI" severity="high" disposition="mitigate" mitigation_plan="No nsec field, no nsec logs, no nsec storage (D-09)">Boris requests or stores an nsec</threat>
    <threat id="T-02" category="Information Disclosure" component="SessionStore" severity="high" disposition="mitigate" mitigation_plan="Exclude boris_session from backup (D-09)">Identity pair included in cloud/device backup</threat>
    <threat id="T-03" category="Spoofing" component="RemoteSignerBridge" severity="medium" disposition="mitigate" mitigation_plan="Omit package on first intent; persist extras package (D-02)">Pinning the wrong signer package on first get_public_key</threat>
    <threat id="T-04" category="Information Disclosure" component="AuthViewModel" severity="medium" disposition="mitigate" mitigation_plan="Do not log result/signature extras">Logcat prints signer result extras</threat>
  </threats>
</threat_model>

<tasks>

<task type="tracer">
  <name>End-to-end Amber login on Home</name>
  <files>app/src/main/AndroidManifest.xml, app/src/main/res/values/strings.xml, app/src/main/java/org/dergigi/boris/nostr/RemoteSignerBridge.kt, app/src/main/java/org/dergigi/boris/nostr/SignerResult.kt, app/src/main/java/org/dergigi/boris/nostr/Nip19.kt, app/src/main/java/org/dergigi/boris/data/Session.kt, app/src/main/java/org/dergigi/boris/data/SessionStore.kt, app/src/main/java/org/dergigi/boris/ui/auth/AuthUiState.kt, app/src/main/java/org/dergigi/boris/ui/auth/AuthViewModel.kt, app/src/main/java/org/dergigi/boris/ui/auth/AuthBar.kt, app/src/main/java/org/dergigi/boris/ui/home/HomeScreen.kt, app/src/main/java/org/dergigi/boris/ui/BorisApp.kt</files>
  <read_first>.planning/phases/01-amber-login/01-CONTEXT.md, .planning/phases/01-amber-login/01-PATTERNS.md, app/src/main/java/org/dergigi/boris/ui/home/HomeScreen.kt, app/src/main/java/org/dergigi/boris/ui/BorisApp.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt, app/src/main/AndroidManifest.xml, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt</read_first>
  <action>
Implement the vertical slice under org.dergigi.boris only. Do not touch com.readwithboris. No new Gradle deps. No DataStore, Hilt, nsec field, bunker, sign_event, permissions JSON, or new NavHost route (D-01, D-03, D-06).

1. AndroidManifest.xml: before &lt;application&gt;, add NIP-55 queries (D-08):
   &lt;queries&gt;
     &lt;intent&gt;
       &lt;action android:name="android.intent.action.VIEW" /&gt;
       &lt;category android:name="android.intent.category.BROWSABLE" /&gt;
       &lt;data android:scheme="nostrsigner" /&gt;
     &lt;/intent&gt;
   &lt;/queries&gt;
   Keep existing SEND/VIEW filters.

2. RemoteSignerBridge object:
   - isSignerAvailable(context): queryIntentActivities(Intent.ACTION_VIEW, Uri.parse("nostrsigner:"), 0) is not empty. Query by scheme, not Amber's package.
   - buildGetPublicKeyIntent(): ACTION_VIEW, Uri.parse("nostrsigner:"), extra type=get_public_key, FLAG_ACTIVITY_SINGLE_TOP | FLAG_ACTIVITY_CLEAR_TOP. Do NOT set package (D-02). Do NOT put permissions extra (D-03).

3. SignerResult: parse Activity result extras. Prefer extra "result", fall back to "signature". Rejected if extra "rejected" is true. Cancelled if resultCode is not RESULT_OK and not rejected. Package from extra "package". Normalize identity with Nip19: if value starts with npub1 decode to hex, else treat as hex (D-05). Never log the extra values (T-04).

4. Session data class: pubkeyHex: String, signerPackage: String.
   SessionStore object: prefs name boris_session, keys pubkey_hex and signer_package (D-04). load(context): Session?, save(context, Session), clear(context).

5. AuthUiState sealed: LoggedOut, MissingSigner, LoggedIn(npub: String). AuthViewModel: AndroidViewModel. On init, if SessionStore.load is non-null, LoggedIn(Nip19.npubEncode(hex)) and do not build a get_public_key intent (D-02). If load is null and !isSignerAvailable, MissingSigner. Else LoggedOut.
   connectIntent(): Intent? = buildGetPublicKeyIntent() only when state is LoggedOut (never when LoggedIn).
   onSignerResult(...): on success save Session(hex, package) then LoggedIn(npub). On rejected vs cancelled vs missing, three different string resources (D-10).
   signOut(): SessionStore.clear then LoggedOut (D-03 AUTH-03).

6. AuthBar composable: LoggedOut shows Connect button. MissingSigner shows copy that Amber is missing plus three links: Zapstore first https://zapstore.dev/apps/com.greenart7c3.nostrsigner, then F-Droid https://f-droid.org/packages/com.greenart7c3.nostrsigner/, then GitHub https://github.com/greenart7c3/Amber/releases (D-08, AUTH-04). LoggedIn shows npub (selectable) and Sign out. No nsec field (D-09).

7. HomeScreen: keep URL field + Read (READ-01). Add AuthBar. rememberLauncherForActivityResult(StartActivityForResult) in HomeScreen (or BorisApp). Domain must not startActivity (D-07). Connect click: launcher.launch(viewModel.connectIntent()). One in-flight connect.

8. strings.xml: auth_connect, auth_sign_out, auth_missing_amber, auth_install_zapstore, auth_install_fdroid, auth_install_github, auth_rejected, auth_cancelled.

9. BorisApp Home composable: construct AuthViewModel with viewModel() like ReaderScreen, pass into HomeScreen. Do not change startDestination.
  </action>
  <verify>./gradlew :app:assembleDebug</verify>
  <acceptance_criteria>
    - app/src/main/AndroidManifest.xml contains android:scheme="nostrsigner" and BROWSABLE
    - RemoteSignerBridge.kt contains buildGetPublicKeyIntent and does not contain `package` assignment
    - RemoteSignerBridge.kt contains putExtra("type", "get_public_key") and does not contain permissions
    - SessionStore.kt contains boris_session and pubkey_hex and signer_package
    - AuthViewModel.kt contains signOut and does not call get_public_key when a session exists
    - AuthBar.kt contains https://zapstore.dev/apps/com.greenart7c3.nostrsigner
    - AuthBar.kt does not contain play.google.com
    - HomeScreen.kt still contains onRead and DEFAULT_ARTICLE_URL
    - No file under org.dergigi.boris contains nsec paste or putExtra("nsec"
  </acceptance_criteria>
  <done>Debug APK builds. Home still has Read. Login chrome exists. Queries declared. First intent has no package pin.</done>
</task>

<task type="auto">
  <name>Nip19 helper and official vector test</name>
  <files>app/src/main/java/org/dergigi/boris/nostr/Nip19.kt, app/src/test/java/org/dergigi/boris/nostr/Nip19Test.kt, app/src/test/java/org/dergigi/boris/nostr/SignerResultTest.kt</files>
  <read_first>https://github.com/nostr-protocol/nips/blob/master/19.md, app/src/test/java/org/dergigi/boris/data/UrlExtractorTest.kt, app/src/main/java/org/dergigi/boris/nostr/Nip19.kt</read_first>
  <action>
Complete Nip19.encode/decode for npub only (D-05). Copy JUnit 4 style from UrlExtractorTest.kt. Include at least one official NIP-19 npub/hex pair from nips/19.md (round-trip encode and decode). SignerResultTest: npub1 input becomes 64-char hex; raw hex passes through; rejected extra maps to Rejected; RESULT_CANCELED maps to Cancelled. Do not add secp256k1 Maven deps; bech32 charset is enough for npub.
  </action>
  <verify>./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.nostr.Nip19Test --tests org.dergigi.boris.nostr.SignerResultTest</verify>
  <acceptance_criteria>
    - Nip19.kt contains npubEncode or equivalent and npubDecode or equivalent
    - Nip19Test.kt contains npub1 and a 64-character hex fixture from NIP-19
    - SignerResultTest.kt contains npub1 and rejected
  </acceptance_criteria>
  <done>Nip19Test and SignerResultTest pass.</done>
</task>

<task type="auto">
  <name>Backup exclude and SessionStore tests</name>
  <files>app/src/main/AndroidManifest.xml, app/src/main/res/xml/data_extraction_rules.xml, app/src/main/res/xml/full_backup_content.xml, app/src/test/java/org/dergigi/boris/data/SessionStoreTest.kt</files>
  <read_first>app/src/main/AndroidManifest.xml, .planning/research/PITFALLS.md</read_first>
  <action>
D-09 / T-02: exclude the boris_session prefs from backup. Smallest change: add res/xml/data_extraction_rules.xml and full_backup_content.xml that exclude sharedpref boris_session, and point application android:dataExtractionRules and android:fullBackupContent at them. Do not set allowBackup=false unless those attributes cannot be added. SessionStoreTest: Robolectric is not in the catalog; if JVM cannot touch SharedPreferences without Android, test a parse/normalize helper instead and keep SessionStore I/O thin. Prefer Roboelectric-free: extract session pair serialization (blank hex rejected, both keys required) into a function tested on JVM. ./gradlew :app:test must still pass.
  </action>
  <verify>./gradlew :app:test :app:assembleDebug</verify>
  <acceptance_criteria>
    - AndroidManifest.xml contains dataExtractionRules or fullBackupContent
    - xml backup rules contain boris_session
    - SessionStoreTest.kt or a sibling test rejects blank pubkey_hex
    - ./gradlew :app:test exits 0
  </acceptance_criteria>
  <done>Unit tests pass. Session prefs are excluded from backup. Identity is not in default backup.</done>
</task>

</tasks>

<verification>
- [ ] ./gradlew :app:test exits 0
- [ ] ./gradlew :app:assembleDebug exits 0
- [ ] Manifest has nostrsigner queries
- [ ] Zapstore URL is the first missing-Amber install destination
- [ ] Home Read path still compiles (READ-01)
</verification>

<success_criteria>
- AUTH-01: Connect launches get_public_key without package pin
- AUTH-02: Session survives process death; npub shown; no re-prompt
- AUTH-03: Sign out clears prefs
- AUTH-04: Missing signer shows Zapstore then F-Droid and GitHub
- READ-01: URL field and Read remain on Home
</success_criteria>

## Artifacts this phase produces

- RemoteSignerBridge (object): isSignerAvailable, buildGetPublicKeyIntent
- SignerResult (sealed): Success, Rejected, Cancelled + parse
- Nip19 (object): npubEncode, npubDecode
- Session (data class)
- SessionStore (object): load, save, clear
- AuthUiState, AuthViewModel, AuthBar
- strings auth_*
- data_extraction_rules.xml, full_backup_content.xml
- Nip19Test, SignerResultTest, SessionStoreTest

<output>
After completion, create `.planning/phases/01-amber-login/01-01-SUMMARY.md`
</output>
