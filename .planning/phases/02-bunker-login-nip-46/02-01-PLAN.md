---
phase: 02-bunker-login-nip-46
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/AndroidManifest.xml
  - app/src/main/res/values/strings.xml
  - app/src/main/java/org/dergigi/boris/MainActivity.kt
  - app/src/main/java/org/dergigi/boris/ui/BorisApp.kt
  - app/src/main/java/org/dergigi/boris/ui/home/HomeScreen.kt
  - app/src/main/java/org/dergigi/boris/ui/auth/AuthBar.kt
  - app/src/main/java/org/dergigi/boris/ui/auth/AuthViewModel.kt
  - app/src/main/java/org/dergigi/boris/ui/auth/AuthUiState.kt
  - app/src/main/java/org/dergigi/boris/data/Session.kt
  - app/src/main/java/org/dergigi/boris/data/SessionStore.kt
  - app/src/main/java/org/dergigi/boris/data/SecretBox.kt
  - app/src/main/java/org/dergigi/boris/nostr/BunkerUri.kt
  - app/src/main/java/org/dergigi/boris/nostr/ClientKeypair.kt
  - app/src/main/java/org/dergigi/boris/nostr/Nip44.kt
  - app/src/main/java/org/dergigi/boris/nostr/Nip01Event.kt
  - app/src/main/java/org/dergigi/boris/nostr/RelaySocket.kt
  - app/src/main/java/org/dergigi/boris/nostr/BunkerClient.kt
  - app/src/test/java/org/dergigi/boris/nostr/BunkerUriTest.kt
  - app/src/test/java/org/dergigi/boris/nostr/Nip44Test.kt
  - app/src/test/java/org/dergigi/boris/data/SessionStoreTest.kt
autonomous: true
requirements:
  - AUTH-05
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - READ-01
user_setup:
  - A live bunker token from Amber remote or nsec.app for device UAT of pair / restart / sign out
  - Amber (com.greenart7c3.nostrsigner) still installed so Amber Connect can replace a bunker session
  - Network on the device or emulator (URI relays are wss only)
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - User can paste a valid bunker token (or open one via VIEW), approve the connect on the remote signer, and see their npub on Home
    - After killing and reopening Boris, the same npub is still shown without pasting the bunker URI again
    - User can sign out; Home no longer shows the npub; a later bunker connect is a fresh pairing
    - Amber Connect / missing-Amber install links still work; a successful Amber or bunker login replaces the other
    - User can still paste, share, or open a URL and read while logged out
  artifacts:
    - path: app/src/main/java/org/dergigi/boris/nostr/BunkerUri.kt
      provides: Fail-closed bunker URI parse
      exports: ["BunkerUri", "parse"]
    - path: app/src/main/java/org/dergigi/boris/nostr/BunkerClient.kt
      provides: NIP-46 connect then get_public_key
      exports: ["BunkerClient"]
    - path: app/src/main/java/org/dergigi/boris/data/Session.kt
      provides: Sealed Amber or Bunker identity
    - path: app/src/main/java/org/dergigi/boris/data/SecretBox.kt
      provides: Keystore AES-GCM wrap of client privkey and bunker secret
    - path: app/src/main/java/org/dergigi/boris/ui/auth/AuthBar.kt
      provides: Bunker field beside unchanged Amber chrome
    - path: app/src/main/java/org/dergigi/boris/MainActivity.kt
      provides: VIEW/share split so bunker tokens never enter the reader
  key_links:
    - from: app/src/main/java/org/dergigi/boris/ui/auth/AuthViewModel.kt
      to: app/src/main/java/org/dergigi/boris/nostr/BunkerClient.kt
      via: connectBunker launches pair on Dispatchers.IO then SessionStore.save of Session.Bunker
      pattern: connectBunker
    - from: app/src/main/java/org/dergigi/boris/nostr/Nip44.kt
      to: fr.acinq.secp256k1.Secp256k1
      via: conversation key uses pubKeyTweakMul then bytes 1..32 then HKDF-Extract salt nip44-v2
      pattern: pubKeyTweakMul
    - from: app/src/main/java/org/dergigi/boris/MainActivity.kt
      to: app/src/main/java/org/dergigi/boris/ui/BorisApp.kt
      via: incomingBunker fills Home field; incomingUrl alone may call Routes.reader
      pattern: incomingBunker
  prohibitions:
    - statement: Boris must not request, persist, or log a user private key
      status: resolved
      verification: explicit
    - statement: Do not port the Amethyst signer tree or add a full Nostr SDK
      status: resolved
      verification: explicit
    - statement: Do not add a second pairing scheme or camera capture
      status: resolved
      verification: explicit
    - statement: BunkerClient must send only connect then get_public_key
      status: resolved
      verification: explicit
    - statement: Opening a bunker VIEW/share must fill the Home field and must not start pairing
      status: resolved
      verification: explicit
---

## Phase Goal

**As a** reader, **I want to** pair a bunker from Home, see my npub after restart, and sign out, **so that** Boris never holds my user private key.

<objective>
Ship optional bunker identity on Home beside Amber: paste or VIEW a bunker token, run NIP-46 connect then get_public_key, persist the user hex plus wrapped secrets, show npub, sign out wipes the disposable client key. Amber chrome and ungated reading stay.

Purpose: AUTH-05 plus AUTH-01 AUTH-02 AUTH-03 AUTH-04 READ-01 must not regress.
D-01: NIP-46 client; copy Amethyst login sequence only.
D-02: bunker token paste and VIEW; fill the field; pair on tap.
D-03: connect then get_public_key; metadata name=Boris; empty perms.
D-04: relays from the URI; wss only.
D-05: fail closed with distinct Home messages.
D-06: one identity; save of one kind replaces the other.
D-07: extend boris_session; wrap client privkey and bunker secret.
D-08: sign out is a local wipe; next pair uses a new client key.
D-09: Auth chrome stays on Home; no new route.
D-10: Amber path must not regress.
D-11: never persist or log a user private key.
D-12: validate the bunker token like Amethyst validateBunkerUri.
Output: Thin NIP-46 client under org.dergigi.boris.nostr, sealed Session, Keystore wrap, Home bunker field, JVM tests
</objective>

<execution_context>
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-bunker-login-nip-46/02-CONTEXT.md
@.planning/phases/02-bunker-login-nip-46/02-RESEARCH.md
@.planning/phases/02-bunker-login-nip-46/02-PATTERNS.md
@.planning/phases/01-amber-login/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@app/src/main/java/org/dergigi/boris/data/Session.kt
@app/src/main/java/org/dergigi/boris/data/SessionStore.kt
@app/src/main/java/org/dergigi/boris/ui/auth/AuthViewModel.kt
@app/src/main/java/org/dergigi/boris/ui/auth/AuthBar.kt
@app/src/main/java/org/dergigi/boris/ui/BorisApp.kt
@app/src/main/java/org/dergigi/boris/MainActivity.kt
@app/src/main/java/org/dergigi/boris/ui/home/HomeScreen.kt

<interfaces>
From app/src/main/java/org/dergigi/boris/data/Session.kt (replace with sealed Amber | Bunker per D-07):
data class Session(val pubkeyHex: String, val signerPackage: String)
companion fromStored(hex: String?, pkg: String?): Session?

From app/src/main/java/org/dergigi/boris/data/SessionStore.kt:
PREFS_NAME = "boris_session"
KEY_PUBKEY_HEX = "pubkey_hex"
KEY_SIGNER_PACKAGE = "signer_package"
load(context): Session?
save(context, Session)
clear(context)

From app/src/main/java/org/dergigi/boris/ui/auth/AuthUiState.kt:
LoggedOut | MissingSigner | LoggedIn(npub)

From app/src/main/java/org/dergigi/boris/ui/auth/AuthViewModel.kt:
connectIntent(): Intent?          // LoggedOut only; keep
onSignerResult(resultCode, data)  // save Session.Amber; replaces bunker
signOut()                         // SessionStore.clear then readState
readState()                       // prefs only; never reopen a bunker socket

From app/src/main/java/org/dergigi/boris/ui/auth/AuthBar.kt:
AuthBar(state, message, onConnect, onSignOut, modifier)
AMBER_ZAPSTORE_URL / AMBER_FDROID_URL / AMBER_GITHUB_URL must stay

From app/src/main/java/org/dergigi/boris/nostr/RemoteSignerBridge.kt (do not edit):
isSignerAvailable(context): Boolean
buildGetPublicKeyIntent(): Intent  // no package extra

From app/src/main/java/org/dergigi/boris/nostr/Nip19.kt (reuse, do not edit):
npubEncode(pubkeyHex): String
normalizePubkey(value): String?

From app/src/main/java/org/dergigi/boris/ui/BorisApp.kt:
BorisApp(incomingUrl: String? = null)
Routes.HOME / Routes.reader(url)  // startDestination stays HOME
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end bunker login on Home</name>
  <precondition>Phase 1 Amber files exist: SessionStore.kt, AuthBar.kt, RemoteSignerBridge.kt, and boris_session.xml backup excludes</precondition>
  <reversibility rating="costly">Sealed Session plus Keystore-wrapped prefs keys are the on-disk identity later phases will read; changing the kind or ciphertext format needs a prefs migration (D-06, D-07).</reversibility>
  <files>gradle/libs.versions.toml, app/build.gradle.kts, app/src/main/AndroidManifest.xml, app/src/main/res/values/strings.xml, app/src/main/java/org/dergigi/boris/MainActivity.kt, app/src/main/java/org/dergigi/boris/ui/BorisApp.kt, app/src/main/java/org/dergigi/boris/ui/home/HomeScreen.kt, app/src/main/java/org/dergigi/boris/ui/auth/AuthBar.kt, app/src/main/java/org/dergigi/boris/ui/auth/AuthViewModel.kt, app/src/main/java/org/dergigi/boris/ui/auth/AuthUiState.kt, app/src/main/java/org/dergigi/boris/data/Session.kt, app/src/main/java/org/dergigi/boris/data/SessionStore.kt, app/src/main/java/org/dergigi/boris/data/SecretBox.kt, app/src/main/java/org/dergigi/boris/nostr/BunkerUri.kt, app/src/main/java/org/dergigi/boris/nostr/ClientKeypair.kt, app/src/main/java/org/dergigi/boris/nostr/Nip44.kt, app/src/main/java/org/dergigi/boris/nostr/Nip01Event.kt, app/src/main/java/org/dergigi/boris/nostr/RelaySocket.kt, app/src/main/java/org/dergigi/boris/nostr/BunkerClient.kt</files>
  <read_first>.planning/phases/02-bunker-login-nip-46/02-CONTEXT.md, .planning/phases/02-bunker-login-nip-46/02-RESEARCH.md, .planning/phases/02-bunker-login-nip-46/02-PATTERNS.md, app/src/main/java/org/dergigi/boris/ui/auth/AuthViewModel.kt, app/src/main/java/org/dergigi/boris/ui/BorisApp.kt, app/src/main/java/org/dergigi/boris/MainActivity.kt, app/src/main/java/org/dergigi/boris/data/Session.kt, app/src/main/java/org/dergigi/boris/data/SessionStore.kt, app/src/main/java/org/dergigi/boris/ui/auth/AuthBar.kt, app/src/main/java/org/dergigi/boris/ui/home/HomeScreen.kt, gradle/libs.versions.toml</read_first>
  <action>
Implement the vertical slice under org.dergigi.boris only. Do not touch com.readwithboris. Do not edit RemoteSignerBridge.kt, SignerResult.kt, Nip19.kt, data_extraction_rules.xml, or full_backup_content.xml. No Hilt, Room, DataStore, or kotlinx.serialization. No new NavHost route D-09:. Copy Amethyst login sequence only: parse token, disposable client key, subscribe kind 24133, connect, get_public_key D-01:. Do not port the Amethyst signer tree or add a full Nostr SDK.

1. Catalog then module (RESEARCH Standard Stack): versions secp256k1 = 0.24.0 and bouncycastle = 1.85.2. Libraries: fr.acinq.secp256k1:secp256k1-kmp, secp256k1-kmp-jni-android, secp256k1-kmp-jni-jvm, org.bouncycastle:bcprov-jdk18on. Wire implementation(libs.secp256k1.kmp), implementation(libs.secp256k1.jni.android), implementation(libs.bouncycastle.bcprov), testImplementation(libs.secp256k1.jni.jvm). Do not register BouncyCastle as a JCE provider; use ChaCha7539Engine.

2. Intent split first (AUTH-05, READ-01) D-02:. AndroidManifest.xml: sibling VIEW filter with android:scheme="bunker" only. Keep http/https VIEW, SEND text/plain, nostrsigner queries, INTERNET, usesCleartextTraffic=false. MainActivity: add incomingBunker state. ACTION_SEND: if EXTRA_TEXT trims to bunker scheme, set incomingBunker and leave incomingUrl null; else UrlExtractor.extract as today. ACTION_VIEW: dataString with bunker scheme goes to incomingBunker, never incomingUrl. BorisApp(incomingUrl, incomingBunker). LaunchedEffect may call Routes.reader only for incomingUrl. Pass incomingBunker into HomeScreen. startDestination stays Routes.HOME.

3. BunkerUri data class: remoteSignerPubkey, relays, secret. parse(input): String to BunkerUri? Fail closed D-11: D-12:. trim; reject NIP-19 secret-key bech32 prefix anywhere (ignore case); require bunker scheme; host is 64-char lowercase hex; at least one relay= value that starts with wss:// after URLDecoder.decode. Ignore ws://. Decode query values. Never persist or log a user private key. No Android types.

4. Session becomes sealed interface with pubkeyHex D-06: D-07:. Session.Amber(pubkeyHex, signerPackage). Session.Bunker(pubkeyHex, remoteSignerPubkey, relays, clientPrivkeyCiphertext, bunkerSecretCiphertext). Keep Session.fromStored(hex, pkg) as Amber-only so existing SessionStoreTest still compiles (blank package still rejected). Bunker restore requires user hex, remote hex, at least one relay, and non-blank client ciphertext. SessionStore same PREFS_NAME boris_session. New keys: kind (amber|bunker), remote_signer_pubkey, relays (comma-separated wss URLs), client_privkey, bunker_secret. Missing kind plus signer_package present loads Amber (existing installs, AUTH-02). save(Amber) writes Amber keys and removes bunker keys plus SecretBox.wipe. save(Bunker) writes bunker keys and removes signer_package. clear() wipes prefs and SecretBox. One identity replaces the other in the same edit().

5. SecretBox object, Context methods wrap / unwrap / wipe. Android Keystore AES-GCM alias boris_bunker_wrap, AES/GCM/NoPadding, 256-bit. Persist iv plus ciphertext strings in boris_session via SessionStore, not a second prefs file. Never log ciphertext or plaintext. Do not unit-test SecretBox on the JVM.

6. Crypto before sockets. ClientKeypair.generate(): 32-byte priv, pubkeyCreate. Reuse nostr toHex / hexToByteArray. Nip44: conversation key is pubKeyTweakMul(remotePub, clientPriv) then bytes 1..32 of the 65-byte uncompressed point, then HKDF-Extract with salt nip44-v2 (javax.crypto.Mac HMAC-SHA256; do not add an HKDF library). Never call the hashed shared-secret helper on Secp256k1. Encrypt v2 ChaCha7539Engine. Always send v2. On receive, if the payload looks like base64-with-iv, decrypt that legacy transport; else v2. Nip01Event: kind 24133, id is SHA-256 of the NIP-01 JSON array with no extra whitespace, signSchnorr, verify incoming before decrypt.

7. BunkerClient + RelaySocket D-03: D-04: D-05:. Inject OkHttpClient like ReaderRepository. One WebSocket per URI relay. Sequence: generate key; REQ kinds=[24133] #p=[clientPub]; wait onOpen 15_000 ms; then EVENT connect; then get_public_key. connect params positional: remote-signer hex, secret or empty, empty perms string, metadata JSON name=Boris and url=https://github.com/dergigi/boris-android. Do not treat connect result as the identity. get_public_key params empty list; normalize with Nip19.normalizePubkey. RPC timeout 65_000 ms each. If result equals auth_url, ACTION_VIEW the URL from the error field once per distinct URL and keep the same request id open. Close sockets after success. Skip the remote logout RPC; local wipe is the boundary D-08:. Sealed BunkerResult: Success(userHex), BadUri, RelayTimeout, Rejected, MissingPubkey. Relays come only from the URI.

8. Auth chrome D-09: D-10: (AUTH-01, AUTH-04). AuthUiState add Connecting. AuthViewModel.connectBunker(uri: String): set Connecting; withContext(Dispatchers.IO) pair; on Success wrap secrets, SessionStore.save(Session.Bunker), LoggedIn(Nip19.npubEncode). Map failures to auth_bunker_bad_uri / auth_bunker_timeout / auth_bunker_rejected / auth_bunker_missing_pubkey on _message. refresh() must not clobber Connecting (auth_url opens a browser; ON_RESUME would otherwise reset). onSignerResult Success saves Session.Amber (clears bunker). connectIntent() still LoggedOut only and still RemoteSignerBridge.buildGetPublicKeyIntent(). signOut() stays SessionStore.clear D-08: (AUTH-03). Restore is prefs only; do not reopen a socket on refresh.

9. AuthBar: keep Amber Connect and the three install links (Zapstore first). Add bunker OutlinedTextField plus Connect bunker on both LoggedOut and MissingSigner. Hide bunker field when LoggedIn. Disable Connect bunker while Connecting. HomeScreen: rememberSaveable bunker field; incoming token fills it; pairing starts only from the bunker button tap D-02:. Keep Amber rememberLauncherForActivityResult, URL field, Read, DEFAULT_ARTICLE_URL (READ-01). Modifier last among layout params.

10. strings.xml add auth_connect_bunker, auth_bunker_hint, auth_bunker_connecting, auth_bunker_bad_uri, auth_bunker_timeout, auth_bunker_rejected, auth_bunker_missing_pubkey. Do not change existing Amber strings.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
    <human-check>On a device with network: paste a bunker token from Amber remote or nsec.app, tap Connect bunker, approve (including auth_url in a browser if shown), confirm npub on Home. Kill and reopen: same npub, no re-paste. Sign out. Confirm Amber Connect still works and replaces bunker. Open a bunker VIEW or share: field fills, reader does not open, pairing does not start until tap. Read an article while logged out.</human-check>
  </verify>
  <acceptance_criteria>
    - AndroidManifest.xml contains android:scheme="bunker" and still contains android:scheme="https"
    - MainActivity.kt contains incomingBunker
    - BorisApp.kt contains incomingBunker and Routes.reader is not called with a bunker-scheme string
    - BunkerUri.kt contains parse and wss://
    - Session.kt contains sealed interface Session and class Bunker and class Amber
    - SessionStore.kt contains kind and client_privkey and SecretBox
    - Nip44.kt contains pubKeyTweakMul
    - BunkerClient.kt contains connect and get_public_key
    - AuthViewModel.kt contains connectBunker and Connecting
    - AuthBar.kt still contains AMBER_ZAPSTORE_URL and auth_connect
    - HomeScreen.kt still contains onRead and DEFAULT_ARTICLE_URL
    - RemoteSignerBridge.kt is unchanged (still contains buildGetPublicKeyIntent)
  </acceptance_criteria>
  <done>Debug APK builds. Home still has Read and Amber chrome. Bunker field exists. VIEW of a bunker token cannot open the reader. Pairing path is connect then get_public_key through OkHttp WebSocket.</done>
</task>

<task type="auto">
  <name>BunkerUri and NIP-44 official vector tests</name>
  <files>app/src/test/java/org/dergigi/boris/nostr/BunkerUriTest.kt, app/src/test/java/org/dergigi/boris/nostr/Nip44Test.kt</files>
  <read_first>app/src/test/java/org/dergigi/boris/data/UrlExtractorTest.kt, app/src/test/java/org/dergigi/boris/nostr/Nip19Test.kt, app/src/main/java/org/dergigi/boris/nostr/BunkerUri.kt, app/src/main/java/org/dergigi/boris/nostr/Nip44.kt, https://github.com/nostr-protocol/nips/blob/master/44.md</read_first>
  <behavior>
    - BunkerUri.parse accepts bunker scheme + 64 hex host + one or more relay=wss values and optional secret
    - BunkerUri.parse returns null for secret-key bech32 prefix anywhere, missing relay, ws:// only, short or non-hex host
    - BunkerUri.parse decodes percent-encoded wss relay values
    - Nip44 conversation key for sec1=00..01 and pub2 from sec2=00..02 equals c41c775356fd92eadc63ff5a0dc1da211b268cbea22316767095b2871ea1412d
    - One official NIP-44 v2 encrypt/decrypt vector round-trips
  </behavior>
  <action>
Add JUnit 4 tests mirroring UrlExtractorTest / Nip19Test (org.junit.Assert, camelCase names, package org.dergigi.boris.nostr). BunkerUriTest: valid token with two relays and a secret; reject secret-key bech32 prefix; reject missing relay; reject non-wss relay; reject short host; decode wss%3A%2F%2F. Nip44Test: pin the official conversation-key vector from nips/44.md (sec1 all-zero-plus-1, conversation_key c41c7753…) before any further crypto work; add one official v2 encrypt/decrypt vector. testImplementation(libs.secp256k1.jni.jvm) must already be on the classpath from the tracer. Do not add Robolectric.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.nostr.BunkerUriTest --tests org.dergigi.boris.nostr.Nip44Test</automated>
  </verify>
  <acceptance_criteria>
    - BunkerUriTest.kt contains parse and wss and a secret-key bech32 fixture
    - Nip44Test.kt contains c41c775356fd92eadc63ff5a0dc1da211b268cbea22316767095b2871ea1412d
    - ./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.nostr.BunkerUriTest --tests org.dergigi.boris.nostr.Nip44Test exits 0
  </acceptance_criteria>
  <done>BunkerUri fail-closed cases and official NIP-44 vectors pass on the JVM.</done>
</task>

<task type="auto">
  <name>Session kind tests and Amber non-regression</name>
  <files>app/src/test/java/org/dergigi/boris/data/SessionStoreTest.kt</files>
  <read_first>app/src/test/java/org/dergigi/boris/data/SessionStoreTest.kt, app/src/main/java/org/dergigi/boris/data/Session.kt, app/src/main/java/org/dergigi/boris/data/SessionStore.kt, app/src/main/res/xml/data_extraction_rules.xml, app/src/main/java/org/dergigi/boris/nostr/RemoteSignerBridge.kt</read_first>
  <action>
Keep the three existing Amber fromStored cases green (valid pair, blank hex, blank package). Add JVM parser tests only (no Robolectric, no live prefs, no Keystore): bunker fromStored accepts user hex + remote hex + relays + wrapped client ciphertext and does not require signer_package; bunker rejects blank client ciphertext; Amber still loads when kind is absent and signer_package is present (AUTH-02). Document in the test names that save of one kind clears the other D-06: even if the clear logic is only asserted via SessionStore key constants / a pure helper. Confirm data_extraction_rules.xml and full_backup_content.xml still exclude boris_session.xml; do not edit them if they already do. Confirm RemoteSignerBridge.kt still has buildGetPublicKeyIntent and still has no package extra (AUTH-01) D-10:. Run the full unit suite and debug assemble.
  </action>
  <verify>
    <automated>./gradlew :app:test :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - SessionStoreTest.kt still contains fromStoredAcceptsValidPair and fromStoredRejectsBlankPackage
    - SessionStoreTest.kt contains a bunker accept case and a bunker reject-blank-ciphertext case
    - app/src/main/res/xml/data_extraction_rules.xml contains boris_session.xml
    - RemoteSignerBridge.kt contains buildGetPublicKeyIntent and putExtra("type", "get_public_key")
    - ./gradlew :app:test :app:assembleDebug exits 0
  </acceptance_criteria>
  <done>Amber session parse still passes. Bunker session parse is fail-closed. Backup exclude unchanged. Full JVM tests and debug APK pass.</done>
</task>

</tasks>

<threat_model asvs="1" block_on="high">

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Home field / VIEW / share → BunkerUri.parse | Untrusted bunker token; may contain a user secret key or a hostile relay |
| Boris process → URI relays (wss) | Untrusted kind 24133 events; ciphertext must not be trusted before verify |
| BunkerClient → auth_url browser | Remote signer supplies a URL; Boris must not substitute its own |
| SecretBox / boris_session → backup and logcat | Client privkey and bunker secret must not leave the device in plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | BunkerUri / AuthBar | high | mitigate | Reject NIP-19 secret-key bech32 anywhere in the input; no private-key field; never persist or log a user key (D-11, D-12) |
| T-02-02 | Information Disclosure | SecretBox / SessionStore | high | mitigate | Keystore AES-GCM wrap of client privkey and bunker secret; keep boris_session.xml backup exclude; no Log; wipe prefs + alias on sign out and on Amber save (D-07, D-08) |
| T-02-03 | Tampering | MainActivity / BorisApp | high | mitigate | Split bunker-scheme VIEW/share onto incomingBunker; Routes.reader only for http(s) (READ-01) |
| T-02-04 | Spoofing | BunkerClient connect | medium | mitigate | Send URI secret on connect; empty requested perms; metadata is name=Boris only (D-03) |
| T-02-05 | Spoofing | BunkerClient auth_url | medium | mitigate | ACTION_VIEW the URL the bunker sent, once per distinct URL; do not inject a Boris-owned URL |
| T-02-06 | Tampering | Nip01Event / Nip44 | medium | mitigate | Verify Schnorr and p-tag before decrypt; send v2 only; pubKeyTweakMul for conversation key |
| T-02-07 | Tampering | SessionStore.save | medium | mitigate | save of one kind clears the other kind's keys and the Keystore alias (D-06) |
| T-02-08 | Information Disclosure | BunkerUri relays | low | mitigate | Require relay=wss://; app already usesCleartextTraffic=false (D-04) |
| T-02-09 | Information Disclosure | signOut | low | accept | Skip remote logout RPC; NIP-46 calls it a courtesy hint; next pair uses a new client key (D-08) |
| T-02-SC | Tampering | Maven installs | high | mitigate | ACINQ secp256k1-kmp 0.24.0 and bcprov-jdk18on 1.85.2 already [OK] in RESEARCH Package Legitimacy Audit; no extra human gate |

</threat_model>

<verification>
- [ ] ./gradlew :app:test exits 0
- [ ] ./gradlew :app:assembleDebug exits 0
- [ ] Manifest has bunker scheme and still has https VIEW plus nostrsigner queries
- [ ] AuthBar still shows Amber Connect or Zapstore-first install links
- [ ] Home Read path still compiles (READ-01)
- [ ] Nip44Test pins conversation_key c41c7753…
</verification>

<success_criteria>
- AUTH-05: Paste or VIEW a bunker token, pair via connect then get_public_key, show npub; Boris never holds a user private key
- AUTH-01: Amber get_public_key intent still has no package pin
- AUTH-02: Restart shows stored npub from SessionStore.load with no Amber prompt and no bunker re-paste
- AUTH-03: Sign out clears prefs and Keystore; next bunker pair is a new client key
- AUTH-04: MissingSigner still shows Zapstore then F-Droid and GitHub, plus the bunker field
- READ-01: URL field and Read remain; bunker VIEW does not open the reader
</success_criteria>

## Artifacts this phase produces

- BunkerUri (data class + parse): remoteSignerPubkey, relays, secret
- ClientKeypair: generate, pub from priv
- Nip44 (object): conversationKey via pubKeyTweakMul, v2 encrypt/decrypt, legacy receive fallback
- Nip01Event: serialize, id, schnorr sign/verify kind 24133
- RelaySocket: OkHttp WebSocket REQ/EVENT/CLOSE per URI relay
- BunkerClient: subscribe, connect, auth_url VIEW, get_public_key, close
- BunkerResult (sealed): Success, BadUri, RelayTimeout, Rejected, MissingPubkey
- SecretBox (object): wrap, unwrap, wipe alias boris_bunker_wrap
- Session (sealed): Amber \| Bunker
- SessionStore keys: kind, remote_signer_pubkey, relays, client_privkey, bunker_secret
- AuthUiState.Connecting
- AuthViewModel.connectBunker
- AuthBar bunker field + Connect bunker
- MainActivity / BorisApp incomingBunker
- strings auth_connect_bunker, auth_bunker_*
- BunkerUriTest, Nip44Test, SessionStoreTest (bunker cases)

<output>
After completion, create `.planning/phases/02-bunker-login-nip-46/02-01-SUMMARY.md`
</output>
