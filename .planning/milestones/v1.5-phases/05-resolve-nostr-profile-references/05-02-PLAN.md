---
phase: 05-resolve-nostr-profile-references
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt
  - app/src/main/java/org/dergigi/boris/MainActivity.kt
  - app/src/main/java/org/dergigi/boris/nostr/RelayQuery.kt
  - app/src/main/java/org/dergigi/boris/ui/you/YouViewModel.kt
  - app/src/main/java/org/dergigi/boris/data/NostrMentions.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt
  - app/src/test/java/org/dergigi/boris/nostr/HintedRelaysTest.kt
  - app/src/test/java/org/dergigi/boris/data/NostrMentionsTest.kt
  - app/src/test/java/org/dergigi/boris/ui/reader/ReaderLinksTest.kt
autonomous: true
requirements:
  - READ-03
  - READ-01
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-08: nprofile type-1 hints are remembered per pubkey in hinted_relays.json and survive later profile-screen visits"
    - "D-07: Kind 0 fetch queries remembered hints plus extraRelays plus existing NIP-65 read / FALLBACK, never replacing the author list"
    - "D-03: remember runs at rewrite and at OpenProfile tap so custom-label links still persist hints"
    - "D-06: Bad or non-wss hints are dropped; pubkey still profiles"
    - "D-01: Mentions still render @name"
    - "D-02: Mentions stay text only"
    - "D-04: Tap still opens Routes.profile logged out"
    - "D-05: note, nevent, naddr, and secret-key URIs stay on 05-01 routing"
    - "D-09: bare bech32 without a nostr scheme stays plaintext"
  artifacts:
    - path: app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt
      provides: per-pubkey extra relays JSON file analog of ReadingPositionStore (D-08)
      exports: ["HintedRelays"]
    - path: app/src/main/java/org/dergigi/boris/nostr/RelayQuery.kt
      provides: fetchProfile extraRelays union with HintedRelays and NIP-65 read (D-07)
      exports: ["fetchProfile"]
  key_links:
    - from: app/src/main/java/org/dergigi/boris/data/NostrMentions.kt
      to: app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt
      via: rewrite calls remember(pubkey, relays)
      pattern: HintedRelays.remember
    - from: app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt
      to: app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt
      via: OpenProfile path remembers Profile.relays (D-03 taps)
      pattern: HintedRelays.remember
    - from: app/src/main/java/org/dergigi/boris/nostr/RelayQuery.kt
      to: app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt
      via: fetchProfileRemote unions forPubkey with extraRelays and fetchRelayList.read
      pattern: HintedRelays.forPubkey
    - from: app/src/main/java/org/dergigi/boris/MainActivity.kt
      to: app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt
      via: init(File(filesDir, "hinted_relays.json")) next to RelayHealth.init
      pattern: HintedRelays.init
  prohibitions:
    - statement: Do not bump versionCode, versionName, CHANGELOG, or publish Zapstore
      status: resolved
      verification: explicit
    - statement: Do not add Gradle libraries
      status: resolved
      verification: explicit
    - statement: Do not introduce a DI framework, a database, or a preferences XML map for this store
      status: resolved
      verification: explicit
    - statement: Do not write hints into kind 10002 events
      status: resolved
      verification: explicit
    - statement: Do not replace NIP-65 relay lists with hints
      status: resolved
      verification: explicit
    - statement: Do not gate profile fetch on login
      status: resolved
      verification: explicit
---

## Phase Goal

`nostr:nprofile` references in article Markdown render as tappable profile links that open the in-app profile screen, without breaking existing note, event, and article nostr links.

<objective>
Persist nprofile relay hints per pubkey and union them into every later kind 0 / profile-screen fetch. D-01: mention labels unchanged. D-02: still text only. D-03: tap persist for custom labels. D-04: profile screen fetch. D-05: secret-key still excluded. D-06: sanitize hints. D-07: union into fetchProfile. D-08: JSON persist. D-09: prefix still required. READ-03 READ-01. 05-01 already paints @name and routes OpenProfile; this plan implements D-08 (costly) and D-07 without changing mention chrome.
Purpose: Without persist, a later visit to ProfileScreen only hits NIP-65 / FALLBACK and can miss kind 0 that lived on the hinted relays.
Output: HintedRelays JSON store, MainActivity init, fetchProfile extraRelays union, remember at rewrite and tap, JVM tests
</objective>

<assumption_delta_decision>
noun: hinted relay persist
decision: new-assumption
rationale: JSON file hinted_relays.json next to RelayHealth, same shape as ReadingPositionStore. Hints are extras unioned with NIP-65; not a fake kind 10002 event.
</assumption_delta_decision>

<execution_context>
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-resolve-nostr-profile-references/05-CONTEXT.md
@.planning/phases/05-resolve-nostr-profile-references/05-RESEARCH.md
@.planning/phases/05-resolve-nostr-profile-references/05-PATTERNS.md
@.planning/phases/05-resolve-nostr-profile-references/05-01-PLAN.md
@.planning/codebase/CONVENTIONS.md
@app/src/main/java/org/dergigi/boris/data/ReadingPositionStore.kt
@app/src/main/java/org/dergigi/boris/MainActivity.kt
@app/src/main/java/org/dergigi/boris/nostr/RelayQuery.kt
@app/src/main/java/org/dergigi/boris/ui/you/YouViewModel.kt
@app/src/main/java/org/dergigi/boris/data/NostrMentions.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt
@app/src/main/java/org/dergigi/boris/nostr/LocalRelays.kt

<interfaces>
From app/src/main/java/org/dergigi/boris/data/ReadingPositionStore.kt (analog, do not copy reading-position fields):
object with init(File), synchronized lock, LinkedHashMap, JSONObject read/write inside runCatching, MAX_ENTRIES eviction of oldest keys. Copy this file I/O, not SessionStore prefs.

From app/src/main/java/org/dergigi/boris/MainActivity.kt onCreate:
EventCache.init, OfflineStore.init, RelayHealth.init(File(filesDir, "relay_health.json")), ReadingPositionStore.init. Add HintedRelays.init(File(filesDir, "hinted_relays.json")) immediately after RelayHealth.init.

From app/src/main/java/org/dergigi/boris/nostr/RelayQuery.kt:
fetchProfile(pubkeyHex) currently caches then fetchProfileRemote. Add extraRelays: List&lt;String&gt; = emptyList() and pass into remote. fetchProfileRemote today uses fetchRelayList(pubkeyHex).read only. Prescribed relay set: (extraRelays + HintedRelays.forPubkey(pubkeyHex) + fetchRelayList(pubkeyHex).read).distinct(). Existing call sites keep working via the default.

From app/src/main/java/org/dergigi/boris/ui/you/YouViewModel.kt refresh:
buildList { addAll(RelayList.FALLBACK); addAll(list.write); addAll(list.read) }.distinct(). Add addAll(HintedRelays.forPubkey(key)). fetchProfile(key) already goes through RelayQuery; extraRelays default is enough there if remote reads HintedRelays.

From app/src/main/java/org/dergigi/boris/data/NostrMentions.kt (from 05-01):
rewrite already decodes Profile pointers. Call HintedRelays.remember(pubkey, relays) on a successful decode.

From app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt (from 05-01):
OpenProfile is returned from NostrLink.parse Profile. Remember target.relays there so D-03 custom labels persist on tap.

From app/src/main/java/org/dergigi/boris/nostr/LocalRelays.kt:
resolve(url) for hygiene. Drop nulls. Never throw.

Gradle: ./gradlew --offline. No version bump. CONVENTIONS: no KDoc, no Log, org.dergigi.boris only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>HintedRelays JSON store and MainActivity init</name>
  <reversibility rating="costly">D-08 is a per-pubkey relay store that later profile fetches read; changing the on-disk map later is a coordinated migration, but CONTEXT already locked JSON-file persist, so do not re-ask.</reversibility>
  <files>app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt, app/src/main/java/org/dergigi/boris/MainActivity.kt</files>
  <read_first>app/src/main/java/org/dergigi/boris/data/ReadingPositionStore.kt, app/src/main/java/org/dergigi/boris/MainActivity.kt, app/src/main/java/org/dergigi/boris/data/OgPreviewCache.kt, app/src/main/java/org/dergigi/boris/nostr/LocalRelays.kt, .planning/phases/05-resolve-nostr-profile-references/05-PATTERNS.md</read_first>
  <action>
Create object HintedRelays in nostr/ as the D-08: store. D-01: D-02: D-03: D-04: D-05: D-06: D-07: D-09: Copy ReadingPositionStore's init/load/synchronous JSONObject write. Do not use a preferences XML map, a database, or a DI framework (CONVENTIONS). Do not write fake kind 10002 events. Do not bump versionCode.

API: init(target: File), remember(pubkeyHex: String, relays: List&lt;String&gt;), forPubkey(pubkeyHex: String): List&lt;String&gt;, internal fun clear() like OgPreviewCache so JVM tests can reset. Keys are lowercase 64-char pubkey hex; ignore blank or wrong-length keys. Values are JSON arrays of sanitized relay URLs. File name hinted_relays.json.

remember (D-06, D-08): union with any existing list for that pubkey, LocalRelays.resolve each URL (drop nulls), distinct(), keep at most MAX_HINTS = 8 extras per pubkey (append order, drop from the front of the extras when over cap). Empty input is a no-op. If file is null (uninit), still update the in-memory map so tests can call init first. Cap the number of pubkeys at 500 like ReadingPositionStore.MAX_ENTRIES, evicting the oldest key.

MainActivity.onCreate: HintedRelays.init(File(filesDir, "hinted_relays.json")) immediately after RelayHealth.init. Do not move other inits.

After verify passes, commit only this task's files with: feat(05-02): persist nprofile relay hints per pubkey
  </action>
  <verify>
    <automated>./gradlew --offline :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - HintedRelays.kt contains object HintedRelays and fun remember and fun forPubkey and fun init and MAX_HINTS
    - HintedRelays.kt contains hinted_relays or writeText
    - MainActivity.kt contains HintedRelays.init
    - grep -vE '^[[:space:]]*(//|#|\*)' app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt | grep -ciE 'androidx.room|dagger.hilt|DataStore' is 0
    - ./gradlew --offline :app:assembleDebug exits 0
  </acceptance_criteria>
  <done>HintedRelays loads and writes a JSON pubkey-to-relays map from MainActivity, with sanitize, union, and cap.</done>
</task>

<task type="auto">
  <name>Union hints into fetchProfile, profile screen, rewrite, and tap</name>
  <files>app/src/main/java/org/dergigi/boris/nostr/RelayQuery.kt, app/src/main/java/org/dergigi/boris/ui/you/YouViewModel.kt, app/src/main/java/org/dergigi/boris/data/NostrMentions.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt</files>
  <read_first>app/src/main/java/org/dergigi/boris/nostr/RelayQuery.kt, app/src/main/java/org/dergigi/boris/ui/you/YouViewModel.kt, app/src/main/java/org/dergigi/boris/data/NostrMentions.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderLinks.kt, app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt</read_first>
  <action>
Wire D-07: and D-08: into the 05-01 mention path. D-01: D-02: D-03: D-04: D-05: D-06: D-09: Do not add a second kind-0 fetch. Do not replace NIP-65 lists. Login is not required (READ-01, D-04). Keep D-01 D-02 D-05 D-09 behavior from 05-01.

1. RelayQuery.fetchProfile(pubkeyHex, extraRelays: List&lt;String&gt; = emptyList()). fetchProfileRemote unions extraRelays + HintedRelays.forPubkey(pubkeyHex) + fetchRelayList(pubkeyHex).read, then distinct(). Cached stale-while-revalidate path stays; pass extraRelays into the refresh lambda. Existing call sites compile with the default.

2. YouViewModel.refresh relay buildList: addAll(HintedRelays.forPubkey(key)) so a later profile-screen visit uses remembered hints (D-08). Keep FALLBACK, write, and read.

3. NostrMentions.rewrite: on a successful profile decode, HintedRelays.remember(pointer.pubkey, pointer.relays) so article paint persists nprofile hints (D-08). Still skip ](url) destinations (D-03) and still require the nostr scheme (D-09).

4. readerLinkAction: when parse is Profile, HintedRelays.remember(target.pubkeyHex, target.relays) then return OpenProfile. Custom-label taps never go through rewrite, so this is the D-03 persist path.

After verify passes, commit only this task's files with: feat(05-02): union remembered relay hints into profile fetch
  </action>
  <verify>
    <automated>./gradlew --offline :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - RelayQuery.kt contains extraRelays and HintedRelays.forPubkey
    - YouViewModel.kt contains HintedRelays.forPubkey
    - NostrMentions.kt contains HintedRelays.remember
    - ReaderLinks.kt contains HintedRelays.remember
    - ./gradlew --offline :app:assembleDebug exits 0
  </acceptance_criteria>
  <done>Kind 0 and profile-screen fetches union remembered hints. Rewrite and OpenProfile both call remember.</done>
</task>

<task type="auto">
  <name>JVM tests for hint persist, cap, and remember call sites</name>
  <files>app/src/test/java/org/dergigi/boris/nostr/HintedRelaysTest.kt, app/src/test/java/org/dergigi/boris/data/NostrMentionsTest.kt, app/src/test/java/org/dergigi/boris/ui/reader/ReaderLinksTest.kt</files>
  <read_first>app/src/test/java/org/dergigi/boris/data/OgPreviewCacheTest.kt, app/src/test/java/org/dergigi/boris/data/NostrMentionsTest.kt, app/src/test/java/org/dergigi/boris/ui/reader/ReaderLinksTest.kt, app/src/main/java/org/dergigi/boris/nostr/HintedRelays.kt</read_first>
  <action>
Add HintedRelaysTest under app/src/test/java/org/dergigi/boris/nostr/ matching OgPreviewCacheTest (temp File, @After clear and delete, JUnit 4). D-06: D-07: D-08: Call HintedRelays.init(temp) at the start of each test. Cover D-06 D-07 D-08: union merge; LocalRelays-rejected URL dropped while pubkey still has the good wss hint; cap at 8; remember then forPubkey; reload from the same File after a second init (clear memory first). Use a 64-char hex pubkey (the official nprofile pubkey from 05-01 tests is fine).

Extend NostrMentionsTest: rewrite of a raw nostr:nprofile with a wss hint leaves that hint in HintedRelays.forPubkey after init. Extend ReaderLinksTest: OpenProfile on a nostr:nprofile with relays calls remember so forPubkey is non-empty (D-03 tap path). Init the temp file in those tests too, and clear after.

Do not add Robolectric. Run with ./gradlew --offline.

After verify passes, commit only this task's test files with: test(05-02): cover hinted relay persist and remember
  </action>
  <verify>
    <automated>./gradlew --offline :app:testDebugUnitTest --tests org.dergigi.boris.nostr.HintedRelaysTest --tests org.dergigi.boris.data.NostrMentionsTest --tests org.dergigi.boris.ui.reader.ReaderLinksTest</automated>
  </verify>
  <acceptance_criteria>
    - HintedRelaysTest.kt contains remember and forPubkey and MAX_HINTS or cap
    - NostrMentionsTest.kt contains HintedRelays
    - ReaderLinksTest.kt contains HintedRelays or remember
    - ./gradlew --offline :app:testDebugUnitTest --tests org.dergigi.boris.nostr.HintedRelaysTest --tests org.dergigi.boris.data.NostrMentionsTest --tests org.dergigi.boris.ui.reader.ReaderLinksTest exits 0
  </acceptance_criteria>
  <done>Hint persist, sanitization, cap, reload, rewrite-remember, and tap-remember tests pass on the JVM with --offline.</done>
</task>

</tasks>

<threat_model asvs="1" block_on="high">

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nprofile type-1 strings → HintedRelays | Untrusted relay URLs persist and are queried later |
| HintedRelays → RelayQuery.fetchProfileRemote | Extra relays join the kind 0 query set |
| hinted_relays.json on disk | Local file can be large or contain junk URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Denial of Service | HintedRelays.remember | high | mitigate | LocalRelays.resolve (wss or local only); MAX_HINTS 8; max 500 pubkeys; distinct() (D-06, D-08) |
| T-05-07 | Tampering | RelayQuery.fetchProfileRemote | high | mitigate | Union hints with NIP-65 read / extraRelays; never replace the author list (D-07) |
| T-05-08 | Information Disclosure | HintedRelays | high | mitigate | Keys are pubkey hex only; never persist a private key; rewrite regex still excludes secret-key HRP from 05-01 (D-05) |
| T-05-09 | Denial of Service | YouViewModel.refresh | medium | mitigate | Same union + RelayHealth cooldown already skips dead relays; no second fetch path |
| T-05-SC | Tampering | Gradle installs | high | mitigate | No new packages; Package Legitimacy Audit is empty |

</threat_model>

<verification>
- [ ] ./gradlew --offline :app:assembleDebug exits 0
- [ ] ./gradlew --offline :app:testDebugUnitTest --tests org.dergigi.boris.nostr.HintedRelaysTest --tests org.dergigi.boris.data.NostrMentionsTest --tests org.dergigi.boris.ui.reader.ReaderLinksTest exits 0
- [ ] MainActivity initializes HintedRelays from hinted_relays.json
- [ ] fetchProfileRemote unions extraRelays, forPubkey, and NIP-65 read
- [ ] Reading still works logged out (READ-01)
</verification>

<success_criteria>
- D-08: hinted relays for a pubkey are remembered on disk and used on later profile visits
- D-07: kind 0 query is hints plus existing profile relays, not a fork
- D-06: bad hints drop; pubkey remains
- D-03: custom-label taps persist hints via readerLinkAction
- D-01 D-02 D-04 D-05 D-09: mention rendering and routing from 05-01 unchanged
- READ-03 READ-01: profile links still open in-app logged out; profile URI still not an article
</success_criteria>

## Artifacts this phase produces

- HintedRelays JSON store (D-08)
- MainActivity hinted_relays.json init
- fetchProfile extraRelays union (D-07)
- YouViewModel hint union
- remember at rewrite and OpenProfile tap
- HintedRelaysTest plus extended NostrMentionsTest / ReaderLinksTest

## API coverage

No new external API. Kind 0 still uses existing RelayQuery.query.

<output>
After completion, create `.planning/phases/05-resolve-nostr-profile-references/05-02-SUMMARY.md`
</output>
