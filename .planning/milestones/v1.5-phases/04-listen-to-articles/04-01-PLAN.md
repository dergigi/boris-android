---
phase: 04-listen-to-articles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/org/dergigi/boris/data/UserSettings.kt
  - app/src/main/java/org/dergigi/boris/tts/TtsLanguage.kt
  - app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt
  - app/src/main/java/org/dergigi/boris/tts/TtsPlaybackService.kt
  - app/src/main/java/org/dergigi/boris/tts/TtsPreview.kt
  - app/src/main/java/org/dergigi/boris/tts/TtsSpeed.kt
  - app/src/main/java/org/dergigi/boris/tts/TtsText.kt
  - app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/test/java/org/dergigi/boris/data/UserSettingsTest.kt
  - app/src/test/java/org/dergigi/boris/tts/TtsLanguageTest.kt
  - app/src/test/java/org/dergigi/boris/tts/TtsSpeedTest.kt
  - app/src/test/java/org/dergigi/boris/tts/TtsTextTest.kt
autonomous: true
requirements:
  - TTS-01
  - READ-01
user_setup:
  - A device or emulator with a system TTS engine installed, for the human-check of speech
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - User can play and pause the current article from the reader top bar; Stop is on the notification and lock screen, not an in-app button (D-16, D-04, ROADMAP-1)
    - Speech continues with the screen off and while browsing other screens; lock-screen and notification can play, pause, stop, and skip paragraph (D-01, D-02, D-04, ROADMAP-2)
    - Play on a different article switches; browsing without play does not interrupt (D-03, ROADMAP-3)
    - Listening works logged out; reading still works if TTS is missing (TTS-01, READ-01, D-11, ROADMAP-6)
    - Play starts at the paragraph nearest the saved reading position, or from the top if none (D-14)
    - While speaking, volume keys change volume and do not scroll the article (D-19)
    - Empty: Listen stays visible when there is no speakable text; tap shows Nothing to read aloud / This article has no speakable text.
    - Loading: First Play starts the service; top-bar icon stays PlayArrow with no spinner; duplicate taps during init are ignored
    - Error: Engine or language failure keeps Listen visible, shows the matching error copy plus Open settings starting ACTION_TEXT_TO_SPEECH_SETTINGS, and does not change the reading layout (D-11)
    - Zero-one-many: skip previous on the first paragraph is a no-op; skip next on the last paragraph ends the session
  artifacts:
    - path: app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt
      provides: Process-wide StateFlow session; start pause resume stop skip setRate preview
      exports: ["TtsPlayback", "TtsSession"]
    - path: app/src/main/java/org/dergigi/boris/tts/TtsPlaybackService.kt
      provides: mediaPlayback FGS, MediaSession, TextToSpeech, audio focus (D-01, D-05)
      exports: ["TtsPlaybackService"]
    - path: app/src/main/java/org/dergigi/boris/tts/TtsText.kt
      provides: Speakable paragraphs and startIndex from reading-position fraction
      exports: ["TtsText"]
    - path: app/src/main/java/org/dergigi/boris/tts/TtsSpeed.kt
      provides: Webapp speed presets and cycle (D-08)
      exports: ["TtsSpeed"]
    - path: app/src/main/java/org/dergigi/boris/tts/TtsLanguage.kt
      provides: Mode resolve, locales, script heuristic (D-09)
      exports: ["TtsLanguage"]
    - path: app/src/main/java/org/dergigi/boris/tts/TtsPreview.kt
      provides: Locked preview sentence (D-10)
      exports: ["TtsPreview"]
    - path: app/src/main/java/org/dergigi/boris/data/UserSettings.kt
      provides: Typed TTS accessors including ttsFollowAlong
  key_links:
    - from: app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
      to: app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt
      via: Top-bar Listen collects TtsPlayback and calls start/pause/resume; error copy plus Open settings starts ACTION_TEXT_TO_SPEECH_SETTINGS
      pattern: TtsPlayback
    - from: app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt
      to: app/src/main/java/org/dergigi/boris/tts/TtsPlaybackService.kt
      via: startForegroundService after session mutate; service owns TextToSpeech
      pattern: TtsPlaybackService
    - from: app/src/main/java/org/dergigi/boris/tts/TtsPlaybackService.kt
      to: app/src/main/AndroidManifest.xml
      via: exported false mediaPlayback service plus TTS_SERVICE queries
      pattern: foregroundServiceType
  prohibitions:
    - statement: Do not put the speech engine in ReaderViewModel or a Composable
      status: resolved
      verification: explicit
    - statement: Do not add a second Activity
      status: resolved
      verification: explicit
    - statement: Do not add Gradle libraries this phase
      status: resolved
      verification: explicit
    - statement: Do not request, persist, or log a user private key
      status: resolved
      verification: explicit
    - statement: Do not put article body in notifications, Toasts, or logcat
      status: resolved
      verification: explicit
    - statement: Do not hide the Listen control when the engine is missing
      status: resolved
      verification: explicit
    - statement: Do not gate reading or Listen on login
      status: resolved
      verification: explicit
---

## Phase Goal

User can listen to the currently open article with on-device TTS, including background playback, webapp-matched settings, and follow-along in the reader.

<objective>
Prove the listen architecture end-to-end: Ready-article Listen in the reader top bar starts process-wide on-device speech that survives leaving the reader and screen-off, with lock-screen transport. The 14-file count is accepted: engine (TtsPlaybackService) and reader Listen must ship together in this wave so the tracer can speak. Settings UI, follow-along paint, and the mini player come in later plans; this plan ships the real engine and the one user path. D-01 D-02 D-03 D-04 D-05 D-08 D-09 D-10 D-11 D-14 D-16 D-19. TTS-01 READ-01.
Purpose: A dead engine in ReaderViewModel would break D-02; this tracer owns playback in TtsPlayback plus TtsPlaybackService before chrome expands.
Output: TtsPlayback, TtsPlaybackService, speakable-text helpers, typed UserSettings TTS accessors, top-bar Listen, JVM tests
</objective>

<assumption_delta_decision>
noun: playback owner
decision: new-assumption
rationale: Speech must outlive ReaderViewModel (D-02). Own it in object TtsPlayback plus a mediaPlayback foreground service. Do not store engine state only in composition.
</assumption_delta_decision>

<execution_context>
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/boris-android/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-listen-to-articles/04-CONTEXT.md
@.planning/phases/04-listen-to-articles/04-RESEARCH.md
@.planning/phases/04-listen-to-articles/04-PATTERNS.md
@.planning/phases/04-listen-to-articles/04-UI-SPEC.md
@.planning/phases/04-listen-to-articles/COVERAGE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@app/src/main/AndroidManifest.xml
@app/src/main/java/org/dergigi/boris/data/UserSettings.kt
@app/src/main/java/org/dergigi/boris/data/SettingsSync.kt
@app/src/main/java/org/dergigi/boris/data/ReadableContent.kt
@app/src/main/java/org/dergigi/boris/data/ReadingPositionStore.kt
@app/src/main/java/org/dergigi/boris/data/MarkdownInline.kt
@app/src/main/java/org/dergigi/boris/data/Footnotes.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt
@app/src/main/java/org/dergigi/boris/ui/reader/VolumeKeys.kt
@app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt

<interfaces>
From app/src/main/java/org/dergigi/boris/data/ReadableContent.kt (do not edit):
data class ReadableContent(url, title, markdown, html, summary, authorPubkey, ...)
val body: String

From app/src/main/java/org/dergigi/boris/data/ReadingPositionStore.kt (do not edit):
fraction(url): Float  // 0..1, 0 if none
Use this at play time for D-14. Do not invent a paragraph-index store.

From app/src/main/java/org/dergigi/boris/data/MarkdownInline.kt (do not edit):
object MarkdownInline { fun plain(text: String): String }

From app/src/main/java/org/dergigi/boris/data/Footnotes.kt (do not edit):
object Footnotes { fun expand(markdown: String): String }

From app/src/main/java/org/dergigi/boris/data/SettingsSync.kt (do not edit the object shape):
object SettingsSync { val settings: StateFlow&lt;UserSettings&gt;; fun apply(next) }
TtsPlayback reads SettingsSync.settings.value for rate and language. Do not add a second settings store.

From app/src/main/java/org/dergigi/boris/data/UserSettings.kt:
Add typed getters next to volumeButtonScroll. Keep withDouble / withString / withBoolean.
Existing DEFAULT_JSON already has ttsUseSystemLanguage, ttsDetectContentLanguage, ttsLanguageMode, ttsDefaultSpeed.

From app/src/main/java/org/dergigi/boris/ui/reader/VolumeKeys.kt (do not edit):
Handle(enabled: Boolean = true, onVolume: (up: Boolean) -> Boolean)
Change only the ReaderScreen call site enabled flag.

From app/src/main/java/org/dergigi/boris/ui/reader/ReaderViewModel.kt:
Keep load() setting Ready from Jina alone (READ-01). Do not hold TextToSpeech here.
sealed interface ReaderUiState { Loading | Ready(content) | Error(message, url) }
url: String

From app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt:
TopAppBar actions: SaveLibraryButton (logged-in Ready), then MoreVert. Insert Listen after Save and before More.
VolumeKeys.Handle(enabled = volumeScroll && settings.volumeButtonScroll) today; add && !speaking.
Top-bar title already maxLines 1 + Ellipsis (overflow UI consideration).

From app/src/main/AndroidManifest.xml:
Add FGS permissions, POST_NOTIFICATIONS, TTS_SERVICE queries sibling, TtsPlaybackService exported=false foregroundServiceType=mediaPlayback. Keep the single MainActivity.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>End-to-end listen from the reader top bar</name>
  <reversibility rating="costly">D-01 foreground media service plus MediaSession is a coordinated undo across manifest, service, and every play control; CONTEXT already locked it, so do not re-ask.</reversibility>
  <files>app/src/main/AndroidManifest.xml, app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt, app/src/main/java/org/dergigi/boris/tts/TtsPlaybackService.kt, app/src/main/java/org/dergigi/boris/tts/TtsText.kt, app/src/main/java/org/dergigi/boris/tts/TtsSpeed.kt, app/src/main/java/org/dergigi/boris/tts/TtsLanguage.kt, app/src/main/java/org/dergigi/boris/tts/TtsPreview.kt, app/src/main/java/org/dergigi/boris/data/UserSettings.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt, app/src/main/res/values/strings.xml</files>
  <read_first>.planning/phases/04-listen-to-articles/04-CONTEXT.md, .planning/phases/04-listen-to-articles/04-RESEARCH.md, .planning/phases/04-listen-to-articles/04-PATTERNS.md, .planning/phases/04-listen-to-articles/04-UI-SPEC.md, app/src/main/java/org/dergigi/boris/data/SettingsSync.kt, app/src/main/java/org/dergigi/boris/data/UserSettings.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt, app/src/main/java/org/dergigi/boris/ui/reader/VolumeKeys.kt, app/src/main/AndroidManifest.xml, app/src/main/java/org/dergigi/boris/data/ReadableContent.kt, app/src/main/java/org/dergigi/boris/data/Footnotes.kt, app/src/main/java/org/dergigi/boris/data/MarkdownInline.kt, app/src/main/java/org/dergigi/boris/data/ReadingPositionStore.kt</read_first>
  <action>
Implement one production listen path under org.dergigi.boris only. Do not recreate com.readwithboris. Do not add Gradle libraries. Do not add a second Activity. Do not construct TextToSpeech in ReaderViewModel or in a Composable (D-02). Follow CONVENTIONS.md: no android.util.Log or Timber, and never write paragraph text into a Toast or notification body. Never request or persist a user private key. TtsPlayback must not call SessionStore; login is not required (TTS-01, READ-01).

1. UserSettings typed accessors (D-07, D-08, D-09, D-13): ttsDefaultSpeed Double default 2.1, ttsLanguageMode String default content, ttsUseSystemLanguage Boolean default false, ttsDetectContentLanguage Boolean default true, ttsFollowAlong Boolean default true. Add ttsFollowAlong true to DEFAULT_JSON. Do not rename the four existing speed/language keys.

2. TtsSpeed (D-08): presets 0.8, 1, 1.2, 1.4, 1.6, 1.8, 2, 2.1, 2.4, 2.8, 3. DEFAULT 2.1. cycle(current) snaps unknown values to the nearest preset then advances (pitfall 8, including integer-looking 1 from withDouble). Pass the stored number straight into setSpeechRate. If setSpeechRate fails, keep the stored setting and still speak.

3. TtsLanguage (D-09): MODES list system, content, en-US, en-GB, zh, es, hi, ar, fr, pt, de, ja, ru. resolveLanguage matches TTSControls.tsx: specific locale first, then content detect, then system language. Missing mode: ttsUseSystemLanguage true means system, else ttsDetectContentLanguage not false means content. detectHeuristic is JVM-safe Unicode script (Han zh, Hiragana/Katakana ja, Arabic ar, Devanagari hi, Cyrillic ru) plus compact stopword scores for en/es/fr/pt/de; null falls through to system language. Service on API 29+ may call TextClassifier.detectLanguage on a worker thread and take getLocale(0).language when confidence is usable; on failure use detectHeuristic. setLanguage uses Locale.forLanguageTag for en-US and en-GB, Locale(language) for the others.

4. TtsText: paragraphs(content) = title, then summary, then splitMarkdownBlocks(Footnotes.expand(body)) flattened with MarkdownInline.plain, whitespace collapsed, empties dropped. Drop fenced code, image-only lines, and table rows. Headings, list items, and blank-line paragraphs are skip units. After building a paragraph, if length exceeds TextToSpeech.getMaxSpeechInputLength() (call the method; do not hard-code), split on sentence punctuation then spaces, but keep one logical index for skip/follow-along and speak sub-chunks as p{i}.{j}. startIndex(fraction, count) uses 0.01f noise floor then (fraction * count).toInt() coerced to 0..count-1 (D-14).

5. TtsPreview object with EXAMPLE_TEXT exactly (D-10): Boris aims to be a calm reader app with clean typography, beautiful design, and a focus on readability. Boris does not and will never have ads, trackers, paywalls, subscriptions, or any other distractions.

6. TtsSession fields: url, title, author, paragraphs, index, playing, paused, followAlongEnabled, followAlongPaused, rate, errorMessage. object TtsPlayback with StateFlow&lt;TtsSession?&gt; like SettingsSync. Methods: start(content, startIndex, title, author), pause, resume, stop, skip(delta), setRate(rate), preview(text), setFollowAlongPaused(paused). start writes the session then Context.startForegroundService. One article at a time (D-03): start on a different url stops the current session first. preview is a one-shot of EXAMPLE_TEXT or the given text: leave TtsPlayback.session null for the whole utterance (no TtsSession, no article url); if an article is speaking, stop it first (one engine). Pause is tts.stop() plus paused=true keeping index; resume speaks the current paragraph (restart the paragraph; use onRangeStart offsets when the callback actually fired). skip(+1/-1) QUEUE_FLUSH the new index. Skip previous at 0 is a no-op. Skip next past last index calls stop(). setRate calls setSpeechRate then QUEUE_FLUSH the current paragraph so the new rate applies.

7. TtsPlaybackService (D-01, D-05): ServiceCompat.startForeground with FOREGROUND_SERVICE_TYPE_MEDIA_PLAYBACK within the startForegroundService timeout. Create a low-importance notification channel. Notification.MediaStyle tied to the session token. Metadata title = article title else url; artist = author if non-blank; never paragraph text (D-04). PlaybackState actions PLAY, PAUSE, PLAY_PAUSE, STOP, SKIP_TO_NEXT, SKIP_TO_PREVIOUS. Callback maps to TtsPlayback resume/pause/stop/skip. Compact lock-screen is play/pause plus paragraph skip; Stop stays on expanded/headset. Request AudioFocusRequest AUDIOFOCUS_GAIN with CONTENT_TYPE_SPEECH from the service after startForeground (target 35). Transient loss pauses (do not duck); AUDIOFOCUS_LOSS stops; GAIN resumes only if the loss was transient. TextToSpeech(applicationContext) OnInitListener; UtteranceProgressListener not the deprecated completed listener. speak(CharSequence, QUEUE_FLUSH, Bundle, utteranceId). Prefer a Voice whose isNetworkConnectionRequired is false when getVoices() is available. On init failure, LANG_MISSING_DATA, LANG_NOT_SUPPORTED, or network synthesis error: set errorMessage on the session, keep Listen visible (D-11), do not retry on a cloud engine. On API 33+, request POST_NOTIFICATIONS once on first start; denial must not hide in-app controls or block reading. Stop the service when the session is cleared. exported=false, no intent-filter, no MediaBrowser.

8. Manifest: FOREGROUND_SERVICE, FOREGROUND_SERVICE_MEDIA_PLAYBACK, POST_NOTIFICATIONS; queries intent android.intent.action.TTS_SERVICE next to the existing nostrsigner query; service android:name=".tts.TtsPlaybackService" android:exported="false" android:foregroundServiceType="mediaPlayback".

9. Reader top bar (D-16): on ReaderUiState.Ready only, logged in or out, even when TTS init failed. IconButton 48dp, colorScheme.onSurface, after SaveLibraryButton and before MoreVert. Idle/stopped: PlayArrow, contentDescription Listen to article. Paused: PlayArrow, Resume playback. Speaking this url: Pause, Pause playback. Click: start at TtsText.startIndex(ReadingPositionStore.fraction(url), paragraphs.size) when idle; pause when playing this url; resume when paused on this url. Empty paragraphs: keep the button, show strings tts_empty_heading / tts_empty_body (snackbar or inline, not a Toast of paragraph text). Engine/language error (D-11): keep the button, show tts_error_engine or tts_error_language, and a snackbar or inline action tts_open_settings that starts Intent(Settings.ACTION_TEXT_TO_SPEECH_SETTINGS) with FLAG_ACTIVITY_NEW_TASK. Plan 02 still adds the same deep link under TtsSection. Loading and Error reader states: omit Listen. No spinner on the icon; ignore duplicate taps until OnInitListener finishes. Top bar is play/pause only; Stop stays on the notification per D-04. Do not add skip or speed to the top bar.

10. VolumeKeys.Handle enabled = volumeScroll && settings.volumeButtonScroll && session is not playing (D-19). When disabled, keys reach the system stream. Do not map volume keys to skip.

11. strings.xml: add every Copywriting Contract row from 04-UI-SPEC.md (listen/pause/resume/stop/skip, cycle speed, section title, speed/language/follow-along labels, empty heading/body, both error bodies, Open settings, mini-player fallback Article, Open article, and every language dropdown label). Do not put EXAMPLE_TEXT in strings.xml.

12. Browse without play must not call stop (D-02, D-03). Leaving the reader destination must not stop speech.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug</automated>
    <human-check>Logged out: open an article, tap Listen in the top bar, hear speech, tap Pause, tap Resume (same paragraph). Leave the reader for Home; speech continues. Play a second article; the first stops and the second starts. With the screen off, lock-screen play/pause/skip paragraph works; Stop is on the notification, not the top bar. While speaking on the reader, volume keys change volume and the article does not page. Kill TTS data or use a missing language: Listen stays visible, reading still works, error copy appears, and Open settings starts system TTS settings.</human-check>
  </verify>
  <acceptance_criteria>
    - TtsPlayback.kt contains object TtsPlayback and fun start and fun pause and fun preview
    - TtsPlaybackService.kt contains TextToSpeech and MediaSession and FOREGROUND_SERVICE_TYPE_MEDIA_PLAYBACK
    - TtsText.kt contains paragraphs and startIndex
    - TtsSpeed.kt contains 2.1 and 3.0 or 3
    - TtsLanguage.kt contains resolveLanguage and en-US
    - TtsPreview.kt contains Boris aims to be a calm reader app with clean typography
    - UserSettings.kt contains ttsDefaultSpeed and ttsFollowAlong and ttsLanguageMode
    - ReaderScreen.kt contains tts_listen_to_article or Listen to article
    - ReaderScreen.kt contains ACTION_TEXT_TO_SPEECH_SETTINGS or tts_open_settings
    - AndroidManifest.xml contains FOREGROUND_SERVICE_MEDIA_PLAYBACK and TTS_SERVICE and TtsPlaybackService
    - grep -n exported app/src/main/AndroidManifest.xml | grep TtsPlaybackService -B2 -A2 | grep -c 'exported="false"' is at least 1
    - strings.xml contains tts_listen_to_article and tts_empty_heading and tts_error_engine and tts_open_settings and tts_follow_along
    - ReaderViewModel.kt contains class ReaderViewModel and ReaderUiState
    - grep -vE '^[[:space:]]*(//|#|\*)' gradle/libs.versions.toml app/build.gradle.kts | grep -ci media3 is 0
    - grep -vE '^[[:space:]]*(//|#|\*)' app/src/main/java/org/dergigi/boris/tts/*.kt | grep -c nsec is 0
  </acceptance_criteria>
  <done>Debug APK builds. Ready-article Listen speaks on-device audio from a mediaPlayback service, pause/resume/skip work, speech survives leaving the reader, and the reader still loads while logged out even if the engine is missing.</done>
</task>

<task type="auto" tdd="true">
  <name>JVM tests for spoken text, speed cycle, language, and TTS defaults</name>
  <files>app/src/test/java/org/dergigi/boris/tts/TtsTextTest.kt, app/src/test/java/org/dergigi/boris/tts/TtsSpeedTest.kt, app/src/test/java/org/dergigi/boris/tts/TtsLanguageTest.kt, app/src/test/java/org/dergigi/boris/data/UserSettingsTest.kt</files>
  <read_first>app/src/test/java/org/dergigi/boris/data/UserSettingsTest.kt, app/src/test/java/org/dergigi/boris/data/MarkdownInlineTest.kt, app/src/main/java/org/dergigi/boris/tts/TtsText.kt, app/src/main/java/org/dergigi/boris/tts/TtsSpeed.kt, app/src/main/java/org/dergigi/boris/tts/TtsLanguage.kt, app/src/main/java/org/dergigi/boris/tts/TtsPreview.kt, app/src/main/java/org/dergigi/boris/data/UserSettings.kt</read_first>
  <behavior>
    - TtsText.paragraphs includes title then summary then body; drops fenced code, image-only lines, and table rows; flattens markdown links to labels
    - TtsText.startIndex(0f, n) and startIndex(0.005f, n) return 0; a mid fraction maps into 0..n-1
    - TtsSpeed.cycle walks the locked preset list; cycle of 1 (integer-looking) does not jump to 0.8; unknown 2.3 snaps then advances
    - TtsLanguage.resolveLanguage: en-US stays en-US; content uses detect; system uses systemLang; detect null falls through to systemLang
    - detectHeuristic: Han sample is zh, Hiragana sample is ja, Arabic sample is ar
    - UserSettings.defaults(): ttsDefaultSpeed 2.1, ttsLanguageMode content, ttsUseSystemLanguage false, ttsDetectContentLanguage true, ttsFollowAlong true
    - TtsPreview.EXAMPLE_TEXT equals the locked D-10 sentence
  </behavior>
  <action>
Add JUnit 4 tests under app/src/test/java/org/dergigi/boris/tts/ matching MarkdownInlineTest (org.junit.Assert, camelCase names, no Robolectric). Extend UserSettingsTest.defaultsMatchWebappReadingDisplay with the five TTS defaults (D-07, D-08, D-09, D-13). Pin TtsPreview.EXAMPLE_TEXT to the D-10 sentence in TtsTextTest or a one-liner in TtsLanguageTest; do not paraphrase it. Do not add an androidTest source set. Run the listed tests then assembleDebug.
  </action>
  <verify>
    <automated>./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.tts.TtsTextTest --tests org.dergigi.boris.tts.TtsSpeedTest --tests org.dergigi.boris.tts.TtsLanguageTest --tests org.dergigi.boris.data.UserSettingsTest</automated>
  </verify>
  <acceptance_criteria>
    - TtsTextTest.kt contains paragraphs and startIndex
    - TtsSpeedTest.kt contains cycle and 2.1
    - TtsLanguageTest.kt contains resolveLanguage
    - UserSettingsTest.kt contains ttsDefaultSpeed and ttsFollowAlong
    - ./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.tts.TtsTextTest --tests org.dergigi.boris.tts.TtsSpeedTest --tests org.dergigi.boris.tts.TtsLanguageTest --tests org.dergigi.boris.data.UserSettingsTest exits 0
  </acceptance_criteria>
  <done>Spoken-text, speed, language, and TTS default tests pass on the JVM. Existing UserSettings tests stay green.</done>
</task>

<task type="auto">
  <name>Article switch, skip edges, and D-11 error path</name>
  <files>app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt, app/src/main/java/org/dergigi/boris/tts/TtsPlaybackService.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt</files>
  <read_first>app/src/main/java/org/dergigi/boris/tts/TtsPlayback.kt, app/src/main/java/org/dergigi/boris/tts/TtsPlaybackService.kt, app/src/main/java/org/dergigi/boris/ui/reader/ReaderScreen.kt</read_first>
  <action>
Close the tracer gaps that are still engine behavior, not settings chrome (D-03, D-04, D-11, UI empty/loading/error/zero-one-many).

1. D-03: TtsPlayback.start on a different url stops the current session then starts the new one. Collecting TtsPlayback on a reader whose url is not the session url must not call stop. Opening Home or another tab must not call stop.

2. Zero-one-many: skip(-1) at index 0 leaves index 0 and keeps speaking or paused as-is. skip(+1) at last index stops and clears the session (notification dismissed). A one-paragraph article: skip previous no-op, skip next ends. Empty paragraph list: start does not speak, error/empty copy as in the tracer, Listen remains.

3. D-11: errorMessage for engine missing vs language missing uses tts_error_engine and tts_error_language. Reader shows that copy without changing ReaderUiState. Ready still paints the article. Do not hide Listen. The reader snackbar or inline action tts_open_settings must start Intent(Settings.ACTION_TEXT_TO_SPEECH_SETTINGS) with FLAG_ACTIVITY_NEW_TASK. If that activity is missing, fall back to ACTION_INSTALL_TTS_DATA then generic Settings. Plan 02 keeps the same button under TtsSection.

4. Loading: while OnInitListener has not fired, keep PlayArrow, drop extra start() calls.

5. After stop() or natural end, session is null, top bar shows Listen to article.
  </action>
  <verify>
    <automated>./gradlew :app:assembleDebug :app:testDebugUnitTest --tests org.dergigi.boris.tts.TtsTextTest --tests org.dergigi.boris.tts.TtsSpeedTest --tests org.dergigi.boris.tts.TtsLanguageTest --tests org.dergigi.boris.data.UserSettingsTest</automated>
  </verify>
  <acceptance_criteria>
    - TtsPlayback.kt contains fun skip and fun stop
    - ReaderScreen.kt still contains tts_listen_to_article or Listen to article
    - ReaderScreen.kt contains ACTION_TEXT_TO_SPEECH_SETTINGS or tts_open_settings
    - TtsPlaybackService.kt still contains TextToSpeech
    - ./gradlew :app:assembleDebug exits 0
  </acceptance_criteria>
  <done>Play on another article replaces the session. Skip edges match UI-SPEC. Engine failure cannot hide Listen or block Ready.</done>
</task>

</tasks>

<threat_model asvs="1" block_on="high">

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ReadableContent.body → speak() | Untrusted article text crosses into the system TTS engine |
| TtsPlaybackService → notification / lock screen | Metadata is visible while the device is locked |
| FGS / MediaSession intents | Other apps must not start or bind this service |
| SessionStore | Listen must not become an auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | TtsPlaybackService | high | mitigate | android:exported="false"; no intent-filter; no MediaBrowser (D-01) |
| T-04-02 | Information Disclosure | MediaStyle notification | high | mitigate | METADATA_KEY_TITLE plus author only; never paragraph text |
| T-04-03 | Information Disclosure | TextToSpeech | high | mitigate | Prefer isNetworkConnectionRequired false; on network synthesis error take the D-11 path and do not retry on a cloud engine (D-05) |
| T-04-04 | Information Disclosure | tts/*.kt | high | mitigate | No android.util.Log or Timber; never persist a user private key; TtsPlayback does not call SessionStore |
| T-04-05 | Denial of Service | ReaderViewModel.load | high | mitigate | Listen is optional; TTS failure cannot flip Ready to Error; VolumeKeys still page when not speaking (READ-01, D-11) |
| T-04-06 | Denial of Service | POST_NOTIFICATIONS | low | accept | Media-session notifications are exempt on 13+; denial must not hide in-app Listen. In-app controls remain. |
| T-04-SC | Tampering | Gradle installs | high | mitigate | No new packages; Package Legitimacy Audit is empty |

</threat_model>

<verification>
- [ ] ./gradlew :app:assembleDebug exits 0
- [ ] ./gradlew :app:testDebugUnitTest --tests org.dergigi.boris.tts.TtsTextTest --tests org.dergigi.boris.tts.TtsSpeedTest --tests org.dergigi.boris.tts.TtsLanguageTest --tests org.dergigi.boris.data.UserSettingsTest exits 0
- [ ] ReaderViewModel.kt has no TextToSpeech symbol
- [ ] Manifest TtsPlaybackService is exported false with mediaPlayback
- [ ] Home paste/share/open URL still compiles (READ-01)
</verification>

<success_criteria>
- TTS-01: User can listen to the current article with on-device TTS; login is not required; reading still works if TTS is missing
- READ-01: Paste, share, or open a URL and read while logged out; Listen does not replace or block reading
- D-01 D-02 D-04: Speech survives screen-off and leaving the reader; notification/lock-screen play pause stop skip paragraph
- D-03: Play on another article switches; browse without play does not interrupt
- D-05: android.speech.tts.TextToSpeech only
- D-14 D-16 D-19: Top-bar play/pause Listen, start near saved position, volume keys change volume while speaking; Stop stays on the notification
</success_criteria>

## Artifacts this phase produces

- TtsPlayback process singleton (D-02, D-03)
- TtsPlaybackService mediaPlayback FGS + MediaSession + TextToSpeech + audio focus (D-01, D-04, D-05)
- TtsText paragraphs + startIndex (D-14)
- TtsSpeed presets + cycle (D-08)
- TtsLanguage resolve + heuristic (D-09)
- TtsPreview.EXAMPLE_TEXT (D-10)
- UserSettings TTS accessors including ttsFollowAlong (D-07, D-13)
- Reader top-bar Listen (D-16)
- VolumeKeys disabled while speaking (D-19)
- Copywriting strings for listen chrome
- TtsTextTest, TtsSpeedTest, TtsLanguageTest, UserSettingsTest TTS defaults

## API coverage

No external API integration: on-device android.speech.tts.TextToSpeech and Android MediaSession only.

<output>
After completion, create `.planning/phases/04-listen-to-articles/04-01-SUMMARY.md`
</output>
