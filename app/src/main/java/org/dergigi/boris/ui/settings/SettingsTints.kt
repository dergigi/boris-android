package org.dergigi.boris.ui.settings

import androidx.compose.ui.graphics.Color

/** One muted family per settings card group, like Android Settings. */
internal object SettingsTints {
    val Look = Color(0xFF8D6E63)
    val Places = Color(0xFF5B7C99)
    val About = Color(0xFF78909C)
}
